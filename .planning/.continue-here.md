---
context: general
task: Integration of patient and staff management with treatments
status: in_progress
last_updated: 2026-04-19T19:17:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.

None identified yet.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| TypeScript type mismatches in Vue forms | Union types (string[] \| object[]) created complexity in form state management; led to multiple compile iterations | advisory | Keep reactive state homogeneous—store IDs separately from details; transform only at emit time |
| Frontend API service not matching backend DTO | Services added without type alignment to backend DTOs | advisory | Ensure frontend interface definitions match backend DTO structure before implementation |

<current_state>

**Completed this session:**
1. ✅ Analyzed existing codebase architecture (Vue 3 + NestJS + TypeORM)
2. ✅ Updated backend DTOs to support employee assignments (StaffAssignmentCreateDto)
3. ✅ Enhanced TreatmentService with employee assignment methods:
   - addStaffAssignment()
   - getStaffAssignments()
   - removeStaffAssignment()
   - updateStaffAssignment()
4. ✅ Added API endpoints to TreatmentController for CRUD operations on staff assignments
5. ✅ Updated frontend TreatmentForm.vue with employee multi-select UI
6. ✅ Created treatmentStaffAssignmentApi service layer
7. ✅ Fixed TypeScript compilation errors in both frontend and backend
8. ✅ **Both frontend and backend compile successfully**

**Current State:**
- Frontend: Vite dev server running on localhost:5173
- Backend: NestJS API server running on localhost:3000
- Database: SQLite (backend/database.sqlite) with migrations applied

**Architecture Implemented:**
```
Treatment (1) ─── Patient (1)
        ├── TreatmentSession (many)
        └── TreatmentStaffAssignment (many)
              ├── Staff (reference)
              ├── role (string)
              └── revenuePercentage (decimal)
```

</current_state>

<completed_work>

## Completed Tasks:

1. **Framework Analysis** - Documented current frontend (Vue 3, Naive UI, Pinia) and backend (NestJS, TypeORM) architecture
2. **Backend DTO Enhancement** - Updated `CreateTreatmentDto` to include `staffAssignments?: StaffAssignmentCreateDto[]`
3. **Backend Service Layer** - Added 4 new methods to TreatmentService for employee assignment CRUD
4. **API Controller Layer** - Added 4 REST endpoints to TreatmentController:
   - POST `/treatments/:id/staff-assignments`
   - GET `/treatments/:id/staff-assignments`
   - PUT `/treatments/:id/staff-assignments/:assignmentId`
   - DELETE `/treatments/:id/staff-assignments/:assignmentId`
5. **Frontend UI Component** - Enhanced TreatmentForm.vue with:
   - Employee multi-select dropdown
   - Role configuration panel for each selected employee
   - Form validation rules
   - Staff data loading from backend
6. **Frontend API Service** - Created `treatmentStaffAssignmentApi` with 4 methods matching backend endpoints
7. **TypeScript Compilation** - Fixed all TS errors and achieved clean build for both projects

</completed_work>

<remaining_work>

## Not Yet Completed:

1. **Unit Tests** - Run `npm run test:unit` to verify treatment/staff assignment logic
2. **E2E Tests** - Manual browser testing of complete workflow:
   - Create treatment
   - Select patient
   - Select template
   - Add multiple employees with roles
   - Submit and verify data persistence
3. **Database Verification** - Confirm staff assignments are correctly stored in `treatment_staff_assignments` table
4. **Integration Testing** - Test bidirectional updates (update staff assignment, verify reflected in treatment detail view)
5. **API Documentation** - Update Swagger/OpenAPI docs if applicable
6. **Error Handling** - Add user feedback for:
   - Invalid staff assignments
   - Duplicate staff role assignments
   - Revenue percentage validation

## Optional Enhancements (Post-MVP):
- Bulk staff assignment for multiple treatments
- Staff role templates
- Revenue percentage auto-calculation
- Audit logging for staff assignment changes

</remaining_work>

<decisions_made>

- **One treatment ↔ one patient, one treatment ↔ many employees** — Per requirements; implemented via TreatmentStaffAssignment join table
- **Employee selection at treatment creation** — Simplest UX; avoids separate step; can be extended later for post-creation management
- **Separate staffAssignmentDetails field in emit** — Avoided TypeScript union type complexity; payload includes both IDs (for form) and objects (for backend)
- **Staff roles as free-text strings** — Flexibility for custom roles (primary, assistant, consultant, etc.); can be enum-constrained later if needed

</decisions_made>

<blockers>

- None currently. Frontend and backend both compile cleanly.
- Infrastructure is ready (dev servers running).

</blockers>

## Required Reading (in order)

1. `.planning/.continue-here.md` (this file) — Overall context and current state
2. `backend/src/treatments/entities/treatment.entity.ts` — Core Treatment model with staffAssignments relation
3. `backend/src/treatments/dto/create-treatment.dto.ts` — Input validation schema including StaffAssignmentCreateDto
4. `backend/src/treatments/services/treatment.service.ts` — Business logic for creating/managing treatments and staff assignments
5. `frontend/src/components/TreatmentForm.vue` — UI for patient/employee selection and role configuration
6. `frontend/src/services/treatments-api.ts` — API service layer (new `treatmentStaffAssignmentApi` exported)

## Infrastructure State

- **Frontend Dev Server**: `npm run dev` (http://localhost:5173)
- **Backend API Server**: `npm run start` (http://localhost:3000)
- **Database**: SQLite at `backend/database.sqlite` with schema migrations applied
- **TypeScript**: v5.9.3
- **Vue**: v3.5.25
- **NestJS**: v10.x

## Pre-Execution Critique Required

Design is complete; implementation is done and compiles. No design changes needed at this point.

**Critique focus for next session:**
- Verify form submit flow: Does TreatmentForm.vue properly pass staffAssignmentDetails to parent component?
- Confirm backend receives and processes staffAssignments array correctly
- Check database: Are records created in treatment_staff_assignments table?

<context>

This session focused on building a **basic framework** for treatment-patient-employee relationships per the user's requirements: "一個療程對應一個病人和多個員工" (one treatment ↔ one patient + multiple employees).

Took a full-stack approach:
1. **Data model first**: Mapped existing entities (Treatment, TreatmentStaffAssignment, Staff) and confirmed the structure already supports N:M relationships
2. **Backend DTO & service layer**: Extended CreateTreatmentDto to accept staffAssignments array; added CRUD methods to TreatmentService
3. **Frontend component**: Enhanced TreatmentForm.vue to allow multi-select of employees with role assignment
4. **API service bridge**: Exported treatmentStaffAssignmentApi for future management endpoints

Encountered and resolved TypeScript compilation issues around union types and reactive state; final build is clean.

The framework is **ready for end-to-end testing** but has not yet been validated against live data or user workflows. The next session should focus on E2E testing and identifying any gaps in data persistence.

</context>

<next_action>

**Immediate next steps on resume:**

1. **Run tests**: `npm run test:unit` in both backend and frontend to catch any logic errors
2. **Manual E2E test in browser**:
   - Navigate to http://localhost:5173
   - Fill TreatmentForm: select patient → select template → select 2+ employees → set roles → submit
   - Check browser network tab to confirm POST payload includes staffAssignmentDetails
   - Verify backend creates records in treatment_staff_assignments table
3. **Check database**: Query `SELECT * FROM treatment_staff_assignments WHERE treatmentId = ?` to confirm persistence
4. **Debug any failures** against the requirements (one treatment per patient, multiple employees, role assignment preserved)

If E2E test succeeds, proceed to integrate this form into the full treatment creation workflow and consider UI refinements.

</next_action>
