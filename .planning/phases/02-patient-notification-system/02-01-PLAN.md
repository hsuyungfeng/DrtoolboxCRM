---
phase: 02-patient-notification-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/events/course-started.event.ts
  - backend/src/events/course-completed.event.ts
  - backend/src/treatments/services/treatment-course.service.ts
  - backend/src/treatments/services/treatment-session.service.ts
  - backend/src/treatments/services/treatment-course.service.spec.ts
  - backend/src/treatments/services/treatment-session.service.spec.ts
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03

must_haves:
  truths:
    - "建立療程套餐後，系統 emit course.started 事件（含 courseId、patientId、clinicId）"
    - "每次課程完成後，系統 emit session.completed 事件（含 sessionId、treatmentCourseId、patientId、clinicId）"
    - "最後一次課程完成時，系統 emit course.completed 事件（含 courseId、patientId、clinicId）"
    - "現有 RevenueEventListener 的 session.completed 訂閱不受影響"
  artifacts:
    - path: "backend/src/events/course-started.event.ts"
      provides: "CourseStartedEvent 類別"
      exports: ["CourseStartedEvent"]
    - path: "backend/src/events/course-completed.event.ts"
      provides: "CourseCompletedEvent 類別"
      exports: ["CourseCompletedEvent"]
    - path: "backend/src/treatments/services/treatment-course.service.ts"
      provides: "createCourse 在儲存後 emit course.started"
      contains: "course.started"
    - path: "backend/src/treatments/services/treatment-session.service.ts"
      provides: "completeSession 在最後 session 完成後 emit course.completed"
      contains: "course.completed"
  key_links:
    - from: "treatment-course.service.ts createCourse()"
      to: "eventEmitter.emit('course.started')"
      via: "EventEmitter2 inject"
      pattern: "emit.*course\\.started"
    - from: "treatment-session.service.ts completeSession()"
      to: "eventEmitter.emit('course.completed')"
      via: "在 completedCount === allSessions.length 判斷後"
      pattern: "emit.*course\\.completed"
---

<objective>
為 Phase 2 通知系統建立事件基礎架構。

Purpose: 療程生命週期的三個通知觸發點（開始/進度/完成）依賴後端事件 emit。現有 session.completed 事件已存在，但缺少 course.started 與 course.completed 事件類別與 emit 點。本計劃補齊這兩個缺口，讓後續通知 Listener 可訂閱。
Output: 兩個新事件類別 + treatment-course.service.ts 中新增 course.started emit + treatment-session.service.ts 中新增 course.completed emit + 更新後測試通過。
</objective>

<execution_context>
@/home/hsu/.claude/get-shit-done/workflows/execute-plan.md
@/home/hsu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-patient-notification-system/02-RESEARCH.md

<interfaces>
<!-- 現有事件類別模式（從 backend/src/events/ 提取）-->

// backend/src/events/session-completed.event.ts（現有模式，直接參考）
export class SessionCompletedEvent {
  constructor(
    public readonly sessionId: string,
    public readonly treatmentId: string,
    public readonly clinicId: string,
    public readonly completedAt: Date = new Date(),
  ) {}
}

// backend/src/events/treatment-completed.event.ts（現有模式，直接參考）
export class TreatmentCompletedEvent {
  constructor(
    public readonly treatmentId: string,
    public readonly clinicId: string,
    public readonly completedAt: Date = new Date(),
  ) {}
}

// treatment-session.service.ts Step 5（現有 emit，行 176-182）
this.eventEmitter.emit("session.completed", {
  sessionId: savedSession.id,
  treatmentCourseId: savedSession.treatmentCourseId,
  patientId: session.treatmentCourse.patientId,
  completedAt: new Date(),
  staffAssignments: session.staffAssignments,
});

// treatment-session.service.ts Step 6（行 184-203，course.completed 應插在 manager.save(TreatmentCourse) 之後）
if (completedCount === allSessions.length) {
  const course = await manager.findOne(TreatmentCourse, {
    where: { id: session.treatmentCourseId },
  });
  if (course) {
    course.status = "completed";
    course.completedAt = new Date();
    await manager.save(TreatmentCourse, course);
    // ← 在此行之後插入 emit course.completed
  }
}

// treatment-course.service.ts createCourse() 最後（行 130 之後，事務外）
// 現有：if (pointsRedeemed.greaterThan(0)) { ... redeemPoints ... }
// return course; 之前插入 emit course.started
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任務 1：建立 CourseStartedEvent 與 CourseCompletedEvent 類別</name>
  <files>
    backend/src/events/course-started.event.ts
    backend/src/events/course-completed.event.ts
  </files>
  <read_first>
    - backend/src/events/session-completed.event.ts（模式參考）
    - backend/src/events/treatment-completed.event.ts（模式參考）
  </read_first>
  <behavior>
    - CourseStartedEvent 有 courseId、patientId、clinicId、startedAt 四個欄位
    - CourseCompletedEvent 有 courseId、patientId、clinicId、completedAt 四個欄位
    - 兩者都使用 constructor 參數注入模式，與現有事件類別一致
    - completedAt / startedAt 預設值為 new Date()
  </behavior>
  <action>
建立 backend/src/events/course-started.event.ts：

```typescript
export class CourseStartedEvent {
  constructor(
    public readonly courseId: string,
    public readonly patientId: string,
    public readonly clinicId: string,
    public readonly startedAt: Date = new Date(),
  ) {}
}
```

建立 backend/src/events/course-completed.event.ts：

```typescript
export class CourseCompletedEvent {
  constructor(
    public readonly courseId: string,
    public readonly patientId: string,
    public readonly clinicId: string,
    public readonly completedAt: Date = new Date(),
  ) {}
}
```

不需測試檔——純資料類別，無業務邏輯，由使用端的服務測試覆蓋。
  </action>
  <verify>
    <automated>ls /home/hsu/Desktop/doctor-crm/backend/src/events/course-started.event.ts && ls /home/hsu/Desktop/doctor-crm/backend/src/events/course-completed.event.ts && grep -l "CourseStartedEvent" /home/hsu/Desktop/doctor-crm/backend/src/events/course-started.event.ts && grep -l "CourseCompletedEvent" /home/hsu/Desktop/doctor-crm/backend/src/events/course-completed.event.ts</automated>
  </verify>
  <done>兩個事件類別檔案存在，內含正確的欄位定義</done>
</task>

<task type="auto" tdd="true">
  <name>任務 2：在 treatment-course.service.ts 注入 EventEmitter2 並 emit course.started</name>
  <files>
    backend/src/treatments/services/treatment-course.service.ts
    backend/src/treatments/services/treatment-course.service.spec.ts
  </files>
  <read_first>
    - backend/src/treatments/services/treatment-course.service.ts（完整讀取）
    - backend/src/treatments/services/treatment-session.service.ts（EventEmitter2 注入模式，行 1-38）
    - backend/src/events/course-started.event.ts（剛建立的事件類別）
    - backend/src/treatments/services/treatment-course.service.spec.ts（現有測試，避免破壞）
  </read_first>
  <behavior>
    - 測試：createCourse() 成功後，mockEventEmitter.emit 被以 'course.started' 和正確 payload 呼叫
    - 測試：pointsRedeemed = 0 時（無點數兌換），仍然 emit course.started
    - 測試：pointsRedeemed > 0 時，在點數兌換後仍 emit course.started
    - 測試：emit 的 payload 包含正確的 courseId（savedCourse.id）、patientId（dto.patientId）、clinicId（dto.clinicId）
  </behavior>
  <action>
**步驟 1：修改 treatment-course.service.ts**

在 import 區塊加入：
```typescript
import { EventEmitter2 } from "@nestjs/event-emitter";
import { CourseStartedEvent } from "../../events/course-started.event";
```

在 constructor 加入 EventEmitter2 注入（加在 dataSource 之後）：
```typescript
private readonly eventEmitter: EventEmitter2,
```

完整的 constructor 參數列（按順序）：
```typescript
constructor(
  @InjectRepository(TreatmentCourse)
  private readonly courseRepository: Repository<TreatmentCourse>,
  @InjectRepository(TreatmentSession)
  private readonly sessionRepository: Repository<TreatmentSession>,
  @InjectRepository(StaffAssignment)
  private readonly staffAssignmentRepository: Repository<StaffAssignment>,
  private readonly templateService: TreatmentCourseTemplateService,
  private readonly treatmentProgressService: TreatmentProgressService,
  private readonly pointsService: PointsService,
  private readonly staffService: StaffService,
  private readonly dataSource: DataSource,
  private readonly eventEmitter: EventEmitter2,
) {}
```

在 createCourse() 最底部（return course 之前）加入：
```typescript
// 發出療程開始事件（用於通知系統）
this.eventEmitter.emit(
  'course.started',
  new CourseStartedEvent(course.id, dto.patientId, dto.clinicId),
);

return course;
```

**注意：** emit 必須在 return course 之前，且在 try/catch 點數兌換之後（不受點數成功/失敗影響）。確認 createCourse 函式的 return 語句位置再插入。

**步驟 2：更新 treatment-course.service.spec.ts**

在 providers mock 中加入 EventEmitter2 mock：
```typescript
const mockEventEmitter = {
  emit: jest.fn(),
};
// 在 providers 陣列中：
{ provide: EventEmitter2, useValue: mockEventEmitter }
```

新增測試案例（加在現有 createCourse 測試之後）：
```typescript
it('建立療程後應 emit course.started 事件', async () => {
  mockEventEmitter.emit.mockClear();
  // ... 呼叫 createCourse 的既有 mock 設定 ...
  await service.createCourse(mockDto);
  expect(mockEventEmitter.emit).toHaveBeenCalledWith(
    'course.started',
    expect.objectContaining({
      courseId: expect.any(String),
      patientId: mockDto.patientId,
      clinicId: mockDto.clinicId,
    }),
  );
});
```
  </action>
  <verify>
    <automated>cd /home/hsu/Desktop/doctor-crm/backend && npm test -- --testPathPattern=treatment-course.service --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - treatment-course.service.ts 包含 EventEmitter2 注入
    - grep "course.started" backend/src/treatments/services/treatment-course.service.ts 有結果
    - treatment-course.service.spec.ts 中 course.started emit 測試通過
    - 全部現有測試仍通過
  </done>
</task>

<task type="auto" tdd="true">
  <name>任務 3：在 treatment-session.service.ts 的最後 session 完成後 emit course.completed</name>
  <files>
    backend/src/treatments/services/treatment-session.service.ts
    backend/src/treatments/services/treatment-session.service.spec.ts
  </files>
  <read_first>
    - backend/src/treatments/services/treatment-session.service.ts（完整讀取，特別是 Step 5 與 Step 6，行 172-207）
    - backend/src/events/course-completed.event.ts（剛建立的事件類別）
    - backend/src/treatments/services/treatment-session.service.spec.ts（現有測試，避免破壞）
  </read_first>
  <behavior>
    - 測試：最後一個 session（completedCount === allSessions.length）完成時，emit 'course.completed'
    - 測試：非最後一個 session 完成時，不 emit 'course.completed'
    - 測試：emit payload 包含正確的 courseId（session.treatmentCourseId）、patientId（session.treatmentCourse.patientId）、clinicId（session.clinicId）
    - 測試：現有 'session.completed' emit 在 Step 5 仍正確發出（回歸測試）
  </behavior>
  <action>
**步驟 1：修改 treatment-session.service.ts**

在 import 區塊加入：
```typescript
import { CourseCompletedEvent } from "../../events/course-completed.event";
```

在 completeSession() 的 Step 6 區塊（行 193-203），在 `await manager.save(TreatmentCourse, course)` 之後，`}` 關閉括號之前加入：

```typescript
if (completedCount === allSessions.length) {
  const course = await manager.findOne(TreatmentCourse, {
    where: { id: session.treatmentCourseId },
  });

  if (course) {
    course.status = "completed";
    course.completedAt = new Date();
    await manager.save(TreatmentCourse, course);

    // 發出療程完成事件（用於通知系統）
    this.eventEmitter.emit(
      'course.completed',
      new CourseCompletedEvent(
        course.id,
        session.treatmentCourse.patientId,
        session.clinicId,
      ),
    );
  }
}
```

**注意：** emit 在事務（transaction）內執行——因為 EventEmitter2 是異步的 (`{ async: true }`)，Listener 在事務提交後才執行，不會影響事務完整性。

**步驟 2：更新 treatment-session.service.spec.ts**

新增兩個測試案例：
```typescript
it('最後一個 session 完成時應 emit course.completed', async () => {
  // 設定 allSessions：所有 sessions 都是 completed
  // 呼叫 completeSession
  expect(mockEventEmitter.emit).toHaveBeenCalledWith(
    'course.completed',
    expect.objectContaining({
      courseId: expect.any(String),
      patientId: expect.any(String),
      clinicId: expect.any(String),
    }),
  );
});

it('非最後一個 session 完成時不應 emit course.completed', async () => {
  // 設定 allSessions：尚有 pending sessions
  mockEventEmitter.emit.mockClear();
  await service.completeSession(sessionId, updateDto, clinicId);
  expect(mockEventEmitter.emit).not.toHaveBeenCalledWith(
    'course.completed',
    expect.anything(),
  );
});
```
  </action>
  <verify>
    <automated>cd /home/hsu/Desktop/doctor-crm/backend && npm test -- --testPathPattern=treatment-session.service --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - treatment-session.service.ts 包含 'course.completed' emit（grep 可找到）
    - treatment-session.service.spec.ts 新增的 course.completed 測試通過
    - 原有 session.completed 測試仍通過
    - cd backend && npm test -- --testPathPattern="treatment-(course|session).service" 全部綠燈
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/hsu/Desktop/doctor-crm/backend && npm test -- --testPathPattern="treatment-(course|session).service" 2>&1 | tail -30
```

預期結果：
- treatment-course.service.spec.ts：所有測試通過，包含 course.started emit 測試
- treatment-session.service.spec.ts：所有測試通過，包含 course.completed emit 測試
- 確認 grep：`grep -r "course.started\|course.completed" backend/src/treatments/services/` 兩個檔案都有命中
</verification>

<success_criteria>
1. backend/src/events/course-started.event.ts 存在，含 courseId、patientId、clinicId、startedAt
2. backend/src/events/course-completed.event.ts 存在，含 courseId、patientId、clinicId、completedAt
3. treatment-course.service.ts 在 createCourse() 末尾 emit 'course.started'
4. treatment-session.service.ts 在最後 session 完成後 emit 'course.completed'
5. 兩個 .spec.ts 測試全部通過
6. cd backend && npm test 整體套件無退化
</success_criteria>

<output>
完成後建立 `.planning/phases/02-patient-notification-system/02-01-SUMMARY.md`
</output>
