---
phase: 02-patient-notification-system
plan: "03"
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - backend/src/notifications/listeners/notification-event.listener.ts
  - backend/src/notifications/listeners/notification-event.listener.spec.ts
  - backend/src/notifications/services/notification-scheduler.service.ts
  - backend/src/notifications/services/notification-scheduler.service.spec.ts
  - backend/src/notifications/notifications.module.ts
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "NotificationEventListener 訂閱 course.started 事件，查詢 Patient 並呼叫 sendMultiChannel"
    - "NotificationEventListener 訂閱 session.completed 事件，查詢 Patient 並呼叫 sendMultiChannel"
    - "NotificationEventListener 訂閱 course.completed 事件，查詢 Patient 並呼叫 sendMultiChannel"
    - "單一 Listener 內任一事件處理失敗不影響其他 Listener（如 RevenueEventListener）"
    - "NotificationSchedulerService Cron job 每天 08:00（台北時間）掃描明天的 TreatmentSession 並呼叫 sendMultiChannel"
  artifacts:
    - path: "backend/src/notifications/listeners/notification-event.listener.ts"
      provides: "三個 @OnEvent 訂閱"
      exports: ["NotificationEventListener"]
    - path: "backend/src/notifications/services/notification-scheduler.service.ts"
      provides: "@Cron 預約提醒排程"
      exports: ["NotificationSchedulerService"]
  key_links:
    - from: "NotificationEventListener"
      to: "PatientRepository.findOne()"
      via: "@InjectRepository(Patient)"
      pattern: "patientRepository\\.findOne"
    - from: "NotificationEventListener"
      to: "NotificationService.sendMultiChannel()"
      via: "constructor DI"
      pattern: "notificationService\\.sendMultiChannel"
    - from: "NotificationSchedulerService"
      to: "TreatmentSession（scheduledDate）"
      via: "@InjectRepository(TreatmentSession)"
      pattern: "DATE\\(session\\.scheduledDate\\)"
---

<objective>
建立事件監聽器與排程服務，連接療程生命週期事件與通知發送。

Purpose: Plan 01 補齊了事件 emit，Plan 02 建立了 NotificationService.sendMultiChannel()。本計劃建立橋梁：訂閱這些事件後觸發通知，以及 Cron job 為明天的預約發送提醒。
Output: NotificationEventListener（覆蓋 NOTIF-01/02/03）+ NotificationSchedulerService（覆蓋 NOTIF-04）+ 更新 NotificationsModule。
</objective>

<execution_context>
@/home/hsu/.claude/get-shit-done/workflows/execute-plan.md
@/home/hsu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-patient-notification-system/02-RESEARCH.md
@.planning/phases/02-patient-notification-system/02-01-SUMMARY.md
@.planning/phases/02-patient-notification-system/02-02-SUMMARY.md

<interfaces>
<!-- 事件 payload（由 Plan 01 建立）-->

// session.completed 事件 payload（來自 treatment-session.service.ts 行 176-182）
{
  sessionId: string;
  treatmentCourseId: string;
  patientId: string;
  completedAt: Date;
  staffAssignments: StaffAssignment[];
}

// course.started 事件 payload（CourseStartedEvent）
new CourseStartedEvent(courseId: string, patientId: string, clinicId: string)

// course.completed 事件 payload（CourseCompletedEvent）
new CourseCompletedEvent(courseId: string, patientId: string, clinicId: string)

<!-- NotificationService.sendMultiChannel 簽名（由 Plan 02 建立）-->
async sendMultiChannel(params: {
  patient: Patient;
  eventType: 'course_started' | 'session_completed' | 'course_completed' | 'appointment_reminder';
  title: string;
  message: string;
  relatedEntityId?: string;
}): Promise<void>

<!-- Patient 實體欄位 -->
{ id, clinicId, name, email (nullable), phoneNumber (nullable) }

<!-- RevenueEventListener 模式（直接參考）-->
// backend/src/revenue/listeners/revenue-event.listener.ts
@OnEvent("session.completed", { async: true })
async handleSessionCompletedEvent(event: SessionCompletedEvent) {
  try {
    // ...處理邏輯
  } catch (error) {
    this.logger.error(`處理失敗: ${error.message}`, error.stack);
    // 不拋出：避免中斷其他 listener
  }
}

<!-- TreatmentSession.scheduledDate 欄位-->
// backend/src/treatments/entities/treatment-session.entity.ts
// scheduledDate: datetime (nullable) — SQLite 存為字串，需用 DATE() 函數比較
// completionStatus: 'pending' | 'completed' | 'cancelled'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>任務 1：建立 NotificationEventListener（覆蓋 NOTIF-01/02/03）</name>
  <files>
    backend/src/notifications/listeners/notification-event.listener.ts
    backend/src/notifications/listeners/notification-event.listener.spec.ts
  </files>
  <read_first>
    - backend/src/revenue/listeners/revenue-event.listener.ts（完整讀取——直接複製模式）
    - backend/src/notifications/services/notification.service.ts（sendMultiChannel 簽名）
    - backend/src/events/course-started.event.ts（由 Plan 01 建立）
    - backend/src/events/course-completed.event.ts（由 Plan 01 建立）
    - backend/src/patients/entities/patient.entity.ts（Patient 欄位）
  </read_first>
  <behavior>
    - 測試：handleCourseStarted 收到 course.started 事件時，從 patientRepository 查詢 patient，呼叫 sendMultiChannel with eventType: 'course_started'
    - 測試：handleSessionCompleted 收到 session.completed 事件時，呼叫 sendMultiChannel with eventType: 'session_completed'
    - 測試：handleCourseCompleted 收到 course.completed 事件時，呼叫 sendMultiChannel with eventType: 'course_completed'
    - 測試：patient 不存在時（findOne 回傳 null），早期返回，不呼叫 sendMultiChannel
    - 測試：sendMultiChannel 拋出例外時，Listener catch 並 logger.error，不重新拋出
  </behavior>
  <action>
建立 backend/src/notifications/listeners/ 目錄並建立 notification-event.listener.ts：

```typescript
import { Injectable, Logger } from '@nestjs/common';
import { OnEvent } from '@nestjs/event-emitter';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { Patient } from '../../patients/entities/patient.entity';
import { NotificationService } from '../services/notification.service';
import { CourseStartedEvent } from '../../events/course-started.event';
import { CourseCompletedEvent } from '../../events/course-completed.event';

@Injectable()
export class NotificationEventListener {
  private readonly logger = new Logger(NotificationEventListener.name);

  constructor(
    private readonly notificationService: NotificationService,
    @InjectRepository(Patient)
    private readonly patientRepository: Repository<Patient>,
  ) {}

  /**
   * 處理療程開始事件（NOTIF-01）
   */
  @OnEvent('course.started', { async: true })
  async handleCourseStarted(event: CourseStartedEvent): Promise<void> {
    this.logger.log(`處理 course.started 事件: courseId=${event.courseId}`);
    try {
      const patient = await this.patientRepository.findOne({
        where: { id: event.patientId },
      });
      if (!patient) {
        this.logger.warn(`患者不存在: patientId=${event.patientId}`);
        return;
      }
      await this.notificationService.sendMultiChannel({
        patient,
        eventType: 'course_started',
        title: '療程已開始',
        message: `親愛的 ${patient.name}，您的療程已正式開始，請按預約時間前來接受治療。`,
        relatedEntityId: event.courseId,
      });
    } catch (error) {
      const msg = error instanceof Error ? error.message : String(error);
      const stack = error instanceof Error ? error.stack : undefined;
      this.logger.error(`處理 course.started 通知失敗: ${msg}`, stack);
      // 不拋出：避免影響其他 listener（如 RevenueEventListener）
    }
  }

  /**
   * 處理課程完成事件（NOTIF-02）
   */
  @OnEvent('session.completed', { async: true })
  async handleSessionCompleted(event: {
    sessionId: string;
    treatmentCourseId: string;
    patientId: string;
    completedAt: Date;
  }): Promise<void> {
    this.logger.log(
      `處理 session.completed 通知: sessionId=${event.sessionId}`,
    );
    try {
      const patient = await this.patientRepository.findOne({
        where: { id: event.patientId },
      });
      if (!patient) {
        this.logger.warn(`患者不存在: patientId=${event.patientId}`);
        return;
      }
      await this.notificationService.sendMultiChannel({
        patient,
        eventType: 'session_completed',
        title: '療程進度更新',
        message: `親愛的 ${patient.name}，您完成了一次療程，繼續保持！請留意下次預約時間。`,
        relatedEntityId: event.sessionId,
      });
    } catch (error) {
      const msg = error instanceof Error ? error.message : String(error);
      const stack = error instanceof Error ? error.stack : undefined;
      this.logger.error(`處理 session.completed 通知失敗: ${msg}`, stack);
    }
  }

  /**
   * 處理療程完成事件（NOTIF-03）
   */
  @OnEvent('course.completed', { async: true })
  async handleCourseCompleted(event: CourseCompletedEvent): Promise<void> {
    this.logger.log(`處理 course.completed 事件: courseId=${event.courseId}`);
    try {
      const patient = await this.patientRepository.findOne({
        where: { id: event.patientId },
      });
      if (!patient) {
        this.logger.warn(`患者不存在: patientId=${event.patientId}`);
        return;
      }
      await this.notificationService.sendMultiChannel({
        patient,
        eventType: 'course_completed',
        title: '療程圓滿完成',
        message: `親愛的 ${patient.name}，恭喜您完成了完整的療程！感謝您的信任，期待再次為您服務。`,
        relatedEntityId: event.courseId,
      });
    } catch (error) {
      const msg = error instanceof Error ? error.message : String(error);
      const stack = error instanceof Error ? error.stack : undefined;
      this.logger.error(`處理 course.completed 通知失敗: ${msg}`, stack);
    }
  }
}
```

建立 backend/src/notifications/listeners/notification-event.listener.spec.ts：

```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { getRepositoryToken } from '@nestjs/typeorm';
import { NotificationEventListener } from './notification-event.listener';
import { NotificationService } from '../services/notification.service';
import { Patient } from '../../patients/entities/patient.entity';

const mockNotificationService = {
  sendMultiChannel: jest.fn(),
};

const mockPatientRepository = {
  findOne: jest.fn(),
};

const mockPatient = {
  id: 'patient-123',
  clinicId: 'clinic-001',
  name: '測試患者',
  email: 'test@example.com',
  phoneNumber: '0912345678',
} as Patient;

describe('NotificationEventListener', () => {
  let listener: NotificationEventListener;

  beforeEach(async () => {
    jest.clearAllMocks();
    const module: TestingModule = await Test.createTestingModule({
      providers: [
        NotificationEventListener,
        { provide: NotificationService, useValue: mockNotificationService },
        { provide: getRepositoryToken(Patient), useValue: mockPatientRepository },
      ],
    }).compile();
    listener = module.get<NotificationEventListener>(NotificationEventListener);
  });

  describe('handleCourseStarted（NOTIF-01）', () => {
    it('應查詢患者並呼叫 sendMultiChannel with course_started', async () => {
      mockPatientRepository.findOne.mockResolvedValue(mockPatient);
      await listener.handleCourseStarted({
        courseId: 'course-1',
        patientId: 'patient-123',
        clinicId: 'clinic-001',
        startedAt: new Date(),
      } as any);
      expect(mockNotificationService.sendMultiChannel).toHaveBeenCalledWith(
        expect.objectContaining({
          patient: mockPatient,
          eventType: 'course_started',
          relatedEntityId: 'course-1',
        }),
      );
    });

    it('患者不存在時應早期返回，不呼叫 sendMultiChannel', async () => {
      mockPatientRepository.findOne.mockResolvedValue(null);
      await listener.handleCourseStarted({
        courseId: 'course-1',
        patientId: 'non-existent',
        clinicId: 'clinic-001',
        startedAt: new Date(),
      } as any);
      expect(mockNotificationService.sendMultiChannel).not.toHaveBeenCalled();
    });

    it('sendMultiChannel 拋出例外時不應重新拋出', async () => {
      mockPatientRepository.findOne.mockResolvedValue(mockPatient);
      mockNotificationService.sendMultiChannel.mockRejectedValue(new Error('發送失敗'));
      await expect(
        listener.handleCourseStarted({
          courseId: 'course-1',
          patientId: 'patient-123',
          clinicId: 'clinic-001',
          startedAt: new Date(),
        } as any),
      ).resolves.not.toThrow();
    });
  });

  describe('handleSessionCompleted（NOTIF-02）', () => {
    it('應呼叫 sendMultiChannel with session_completed', async () => {
      mockPatientRepository.findOne.mockResolvedValue(mockPatient);
      await listener.handleSessionCompleted({
        sessionId: 'session-1',
        treatmentCourseId: 'course-1',
        patientId: 'patient-123',
        completedAt: new Date(),
      });
      expect(mockNotificationService.sendMultiChannel).toHaveBeenCalledWith(
        expect.objectContaining({
          eventType: 'session_completed',
          relatedEntityId: 'session-1',
        }),
      );
    });
  });

  describe('handleCourseCompleted（NOTIF-03）', () => {
    it('應呼叫 sendMultiChannel with course_completed', async () => {
      mockPatientRepository.findOne.mockResolvedValue(mockPatient);
      await listener.handleCourseCompleted({
        courseId: 'course-1',
        patientId: 'patient-123',
        clinicId: 'clinic-001',
        completedAt: new Date(),
      } as any);
      expect(mockNotificationService.sendMultiChannel).toHaveBeenCalledWith(
        expect.objectContaining({
          eventType: 'course_completed',
          relatedEntityId: 'course-1',
        }),
      );
    });
  });
});
```
  </action>
  <verify>
    <automated>cd /home/hsu/Desktop/doctor-crm/backend && npm test -- --testPathPattern=notification-event.listener --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - notification-event.listener.ts 存在，含 3 個 @OnEvent 方法
    - notification-event.listener.spec.ts 全部測試通過（含 NOTIF-01/02/03 覆蓋）
    - grep "course.started\|session.completed\|course.completed" 三者都在 Listener 中
  </done>
</task>

<task type="auto" tdd="true">
  <name>任務 2：建立 NotificationSchedulerService（NOTIF-04）並更新 NotificationsModule</name>
  <files>
    backend/src/notifications/services/notification-scheduler.service.ts
    backend/src/notifications/services/notification-scheduler.service.spec.ts
    backend/src/notifications/notifications.module.ts
  </files>
  <read_first>
    - backend/src/treatments/entities/treatment-session.entity.ts（scheduledDate 欄位與 completionStatus 值）
    - backend/src/notifications/services/notification.service.ts（sendMultiChannel 簽名）
    - backend/src/notifications/notifications.module.ts（現有 imports，需加入 ScheduleModule、新 listener 與 scheduler）
    - .planning/phases/02-patient-notification-system/02-RESEARCH.md（模式 4 Cron job + 陷阱 4 SQLite DATE 查詢）
  </read_first>
  <behavior>
    - 測試：sendAppointmentReminders() 以正確日期字串（yyyy-MM-dd）查詢 TreatmentSession
    - 測試：找到明天有 scheduledDate 且 completionStatus='pending' 的 session 時，呼叫 sendMultiChannel with eventType: 'appointment_reminder'
    - 測試：session 無關聯患者（relation 為 null）時，不拋出例外，直接跳過
    - 測試：無明天 session 時，不呼叫 sendMultiChannel
  </behavior>
  <action>
建立 backend/src/notifications/services/notification-scheduler.service.ts：

```typescript
import { Injectable, Logger } from '@nestjs/common';
import { Cron, CronExpression } from '@nestjs/schedule';
import { InjectRepository } from '@nestjs/typeorm';
import { Repository } from 'typeorm';
import { TreatmentSession } from '../../treatments/entities/treatment-session.entity';
import { NotificationService } from './notification.service';

@Injectable()
export class NotificationSchedulerService {
  private readonly logger = new Logger(NotificationSchedulerService.name);

  constructor(
    @InjectRepository(TreatmentSession)
    private readonly sessionRepository: Repository<TreatmentSession>,
    private readonly notificationService: NotificationService,
  ) {}

  /**
   * 每天早上 08:00（台北時間）掃描明天的預約並發送提醒（NOTIF-04）
   * 使用 DATE() 函數處理 SQLite 字串日期欄位（參考 02-RESEARCH.md 陷阱 4）
   */
  @Cron('0 8 * * *', { timeZone: 'Asia/Taipei' })
  async sendAppointmentReminders(): Promise<void> {
    const tomorrow = new Date();
    tomorrow.setDate(tomorrow.getDate() + 1);
    const tomorrowStr = tomorrow.toISOString().split('T')[0]; // 'yyyy-MM-dd'

    this.logger.log(`掃描 ${tomorrowStr} 的預約提醒`);

    try {
      const sessions = await this.sessionRepository
        .createQueryBuilder('session')
        .leftJoinAndSelect('session.treatmentCourse', 'course')
        .leftJoinAndSelect('course.patient', 'patient')
        .where("DATE(session.scheduledDate) = :date", { date: tomorrowStr })
        .andWhere('session.completionStatus = :status', { status: 'pending' })
        .getMany();

      this.logger.log(`找到 ${sessions.length} 個預約提醒`);

      for (const session of sessions) {
        const patient = session.treatmentCourse?.patient;
        if (!patient) {
          this.logger.warn(`session ${session.id} 無關聯患者，跳過`);
          continue;
        }

        try {
          await this.notificationService.sendMultiChannel({
            patient,
            eventType: 'appointment_reminder',
            title: '明日療程提醒',
            message: `親愛的 ${patient.name}，提醒您明天（${tomorrowStr}）有療程預約，請準時前往診所。`,
            relatedEntityId: session.id,
          });
        } catch (error) {
          const msg = error instanceof Error ? error.message : String(error);
          this.logger.error(`發送預約提醒失敗 sessionId=${session.id}: ${msg}`);
          // 繼續處理其他 session，不中斷整個批次
        }
      }
    } catch (error) {
      const msg = error instanceof Error ? error.message : String(error);
      this.logger.error(`預約提醒排程失敗: ${msg}`);
    }
  }
}
```

建立 backend/src/notifications/services/notification-scheduler.service.spec.ts：

```typescript
import { Test, TestingModule } from '@nestjs/testing';
import { getRepositoryToken } from '@nestjs/typeorm';
import { NotificationSchedulerService } from './notification-scheduler.service';
import { NotificationService } from './notification.service';
import { TreatmentSession } from '../../treatments/entities/treatment-session.entity';

const mockSessionRepository = {
  createQueryBuilder: jest.fn(),
};

const mockNotificationService = {
  sendMultiChannel: jest.fn(),
};

const buildQueryBuilder = (sessions: any[]) => ({
  leftJoinAndSelect: jest.fn().mockReturnThis(),
  where: jest.fn().mockReturnThis(),
  andWhere: jest.fn().mockReturnThis(),
  getMany: jest.fn().mockResolvedValue(sessions),
});

describe('NotificationSchedulerService', () => {
  let service: NotificationSchedulerService;

  beforeEach(async () => {
    jest.clearAllMocks();
    const module: TestingModule = await Test.createTestingModule({
      providers: [
        NotificationSchedulerService,
        { provide: NotificationService, useValue: mockNotificationService },
        { provide: getRepositoryToken(TreatmentSession), useValue: mockSessionRepository },
      ],
    }).compile();
    service = module.get<NotificationSchedulerService>(NotificationSchedulerService);
  });

  describe('sendAppointmentReminders', () => {
    it('找到明天 pending session 時，應呼叫 sendMultiChannel', async () => {
      const mockSession = {
        id: 'session-1',
        treatmentCourse: {
          patient: {
            id: 'patient-1',
            clinicId: 'clinic-1',
            name: '測試患者',
            email: 'test@example.com',
            phoneNumber: '0912345678',
          },
        },
      };
      mockSessionRepository.createQueryBuilder.mockReturnValue(
        buildQueryBuilder([mockSession]),
      );

      await service.sendAppointmentReminders();

      expect(mockNotificationService.sendMultiChannel).toHaveBeenCalledWith(
        expect.objectContaining({
          eventType: 'appointment_reminder',
          patient: mockSession.treatmentCourse.patient,
        }),
      );
    });

    it('無明天 session 時，不應呼叫 sendMultiChannel', async () => {
      mockSessionRepository.createQueryBuilder.mockReturnValue(
        buildQueryBuilder([]),
      );
      await service.sendAppointmentReminders();
      expect(mockNotificationService.sendMultiChannel).not.toHaveBeenCalled();
    });

    it('session 無關聯患者時應跳過，不拋出例外', async () => {
      const sessionNoPatient = {
        id: 'session-2',
        treatmentCourse: { patient: null },
      };
      mockSessionRepository.createQueryBuilder.mockReturnValue(
        buildQueryBuilder([sessionNoPatient]),
      );
      await expect(service.sendAppointmentReminders()).resolves.not.toThrow();
      expect(mockNotificationService.sendMultiChannel).not.toHaveBeenCalled();
    });

    it('某個 session 通知失敗時，應繼續處理其他 session', async () => {
      const patient1 = { id: 'p1', clinicId: 'c1', name: '患者一', email: 'a@a.com', phoneNumber: '0911111111' };
      const patient2 = { id: 'p2', clinicId: 'c1', name: '患者二', email: 'b@b.com', phoneNumber: '0922222222' };
      mockSessionRepository.createQueryBuilder.mockReturnValue(
        buildQueryBuilder([
          { id: 's1', treatmentCourse: { patient: patient1 } },
          { id: 's2', treatmentCourse: { patient: patient2 } },
        ]),
      );
      mockNotificationService.sendMultiChannel
        .mockRejectedValueOnce(new Error('第一個失敗'))
        .mockResolvedValueOnce(undefined);

      await expect(service.sendAppointmentReminders()).resolves.not.toThrow();
      expect(mockNotificationService.sendMultiChannel).toHaveBeenCalledTimes(2);
    });
  });
});
```

**更新 notifications.module.ts**：

讀取現有 notifications.module.ts 後，加入以下更改：

```typescript
import { Module } from '@nestjs/common';
import { TypeOrmModule } from '@nestjs/typeorm';
import { ScheduleModule } from '@nestjs/schedule';
import { ChurnPredictionService } from './churn-prediction.service';
import { NotificationService } from './services/notification.service';
import { NotificationSchedulerService } from './services/notification-scheduler.service';
import { NotificationEventListener } from './listeners/notification-event.listener';
import { NotificationsController } from './notifications.controller';
import { NotificationRecord } from './entities/notification-record.entity';
import { NotificationPreference } from './entities/notification-preference.entity';
import { TreatmentSession } from '../treatments/entities/treatment-session.entity';
import { Treatment } from '../treatments/entities/treatment.entity';
import { Patient } from '../patients/entities/patient.entity';
import { PointsBalance } from '../points/entities/points-balance.entity';

@Module({
  imports: [
    ScheduleModule.forRoot(),
    TypeOrmModule.forFeature([
      NotificationRecord,
      NotificationPreference,
      TreatmentSession,
      Treatment,
      Patient,
      PointsBalance,
    ]),
  ],
  providers: [
    ChurnPredictionService,
    NotificationService,
    NotificationSchedulerService,
    NotificationEventListener,
  ],
  controllers: [NotificationsController],
  exports: [ChurnPredictionService, NotificationService],
})
export class NotificationsModule {}
```

**注意：** ScheduleModule.forRoot() 只能在 app 中 import 一次；若 app.module.ts 已有，則移除此處的 ScheduleModule.forRoot()，改為僅在 notifications.module.ts 中列出 NotificationSchedulerService 為 provider。請先 grep app.module.ts 確認是否已有 @nestjs/schedule。
  </action>
  <verify>
    <automated>cd /home/hsu/Desktop/doctor-crm/backend && npm test -- --testPathPattern="notification-(event.listener|scheduler)" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>
    - notification-event.listener.spec.ts 全部 5 個測試通過
    - notification-scheduler.service.spec.ts 全部 4 個測試通過
    - notifications.module.ts 包含 NotificationEventListener 與 NotificationSchedulerService
    - grep "ScheduleModule\|@Cron" backend/src/notifications/services/notification-scheduler.service.ts 有結果
  </done>
</task>

</tasks>

<verification>
```bash
cd /home/hsu/Desktop/doctor-crm/backend && npm test -- --testPathPattern="notification" --passWithNoTests 2>&1 | tail -30
```

預期：notification-event.listener.spec.ts 和 notification-scheduler.service.spec.ts 全部通過。

```bash
cd /home/hsu/Desktop/doctor-crm/backend && npx tsc --noEmit 2>&1 | head -20
```

預期：無 TypeScript 錯誤。
</verification>

<success_criteria>
1. NotificationEventListener 訂閱 course.started、session.completed、course.completed 三個事件
2. 每個事件 handler：查詢 Patient → 呼叫 sendMultiChannel → try/catch 不拋出
3. NotificationSchedulerService 使用 @Cron('0 8 * * *', Asia/Taipei) 每日掃描
4. Cron job 使用 DATE(session.scheduledDate) = :date SQLite 安全查詢
5. notification-event.listener.spec.ts 全部測試通過
6. notification-scheduler.service.spec.ts 全部測試通過
7. notifications.module.ts 包含新的 providers
</success_criteria>

<output>
完成後建立 `.planning/phases/02-patient-notification-system/02-03-SUMMARY.md`
</output>
