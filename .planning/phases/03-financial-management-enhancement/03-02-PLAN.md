---
phase: 03-financial-management-enhancement
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - backend/src/revenue/entities/invoice.entity.ts
  - backend/src/revenue/dto/create-invoice.dto.ts
  - backend/src/revenue/services/invoice.service.ts
  - backend/src/revenue/services/invoice.service.spec.ts
  - backend/src/revenue/controllers/invoice.controller.ts
  - backend/src/revenue/revenue.module.ts
autonomous: true
requirements:
  - FIN-03
  - FIN-04

must_haves:
  truths:
    - "醫護人員能為療程生成發票（一療程一張）"
    - "發票包含患者姓名、療程名稱、金額、付款狀態、開立日期"
    - "發票含費用明細（按付款記錄拆分）"
    - "發票狀態流轉：draft → issued → cancelled"
    - "系統防止重複開立（同一 treatmentId 不得有兩張 issued 發票）"
  artifacts:
    - path: "backend/src/revenue/entities/invoice.entity.ts"
      provides: "Invoice TypeORM 實體"
      contains: "treatmentId, patientId, invoiceNumber, totalAmount, status, issuedAt, clinicId"
    - path: "backend/src/revenue/services/invoice.service.ts"
      provides: "發票 CRUD + 生成邏輯"
      exports: ["create", "issue", "findOne", "findByPatient", "findByTreatment", "cancel"]
    - path: "backend/src/revenue/controllers/invoice.controller.ts"
      provides: "發票 REST API"
      contains: "POST /invoices, GET /invoices/:id, PATCH /invoices/:id/issue, PATCH /invoices/:id/cancel"
  key_links:
    - from: "backend/src/revenue/services/invoice.service.ts"
      to: "backend/src/revenue/services/fee-calculation.service.ts"
      via: "constructor injection"
      pattern: "private feeCalculationService: FeeCalculationService"
    - from: "backend/src/revenue/services/invoice.service.ts"
      to: "backend/src/revenue/entities/payment.entity.ts"
      via: "TypeORM Repository"
      pattern: "@InjectRepository(Payment)"
    - from: "backend/src/revenue/entities/invoice.entity.ts"
      to: "backend/src/treatments/entities/treatment.entity.ts"
      via: "ManyToOne"
      pattern: "@ManyToOne(() => Treatment)"
---

<objective>
建立發票生成系統，滿足台灣稅務需求（FIN-04）並提供費用明細查詢（FIN-03）。

目的：患者完成付款後，醫護人員需生成正式發票，發票須包含療程金額明細（對應付款記錄）、患者資料、診所資料。本計劃在 Plan 01 建立的 Payment 基礎上，新增 Invoice 實體與服務。

輸出成果：
- `invoice.entity.ts` — 發票 TypeORM 實體
- `invoice.service.ts` — 發票生成（含自動流水號）、狀態管理、明細查詢
- `invoice.controller.ts` — REST API 端點
- `invoice.service.spec.ts` — 單元測試
</objective>

<execution_context>
@/home/hsu/.claude/get-shit-done/workflows/execute-plan.md
@/home/hsu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/codebase/CONVENTIONS.md
@.planning/phases/03-financial-management-enhancement/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01 建立的型別（執行前確認 03-01 完成） -->

從 backend/src/revenue/entities/payment.entity.ts（Plan 01 建立）：
```typescript
export class Payment {
  id: string;
  treatmentId: string;
  patientId: string;
  amount: number;       // decimal(20,2)
  paymentMethod: 'cash' | 'bank_transfer' | 'credit_card';
  status: string;       // completed | refunded | cancelled
  paidAt: Date;
  notes: string | null;
  clinicId: string;
  recordedBy: string | null;
}
```

從 backend/src/revenue/services/fee-calculation.service.ts（Plan 01 建立）：
```typescript
async calculateBalance(treatmentId: string, clinicId: string): Promise<{
  totalFee: number;
  totalPaid: number;
  balance: number;
  payments: Payment[];
}>

async calculateTreatmentFee(treatmentId: string, clinicId: string): Promise<{
  totalPrice: number;
  pointsRedeemed: number;
  finalPrice: number;
}>
```

從 backend/src/treatments/entities/treatment.entity.ts：
```typescript
export class Treatment {
  id: string;
  patientId: string;
  clinicId: string;
  name: string;
  totalPrice: number;
  finalPrice: number;
  status: string;
}
```

從 backend/src/patients/entities/patient.entity.ts：
```typescript
export class Patient {
  id: string;
  clinicId: string;
  name: string;
  idNumber: string;
  phone?: string;
  email?: string;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：Invoice 實體與 InvoiceService</name>
  <files>
    backend/src/revenue/entities/invoice.entity.ts
    backend/src/revenue/dto/create-invoice.dto.ts
    backend/src/revenue/services/invoice.service.ts
    backend/src/revenue/services/invoice.service.spec.ts
  </files>

  <read_first>
    - backend/src/revenue/entities/payment.entity.ts （Plan 01，確認 Payment 欄位）
    - backend/src/revenue/services/fee-calculation.service.ts （Plan 01，calculateBalance 簽名）
    - backend/src/treatments/entities/treatment.entity.ts （name、patientId、clinicId）
    - backend/src/patients/entities/patient.entity.ts （name、idNumber、phone）
    - backend/src/revenue/entities/revenue-adjustment.entity.ts （reviewStatus 等狀態模式）
  </read_first>

  <behavior>
    - Test 1：InvoiceService.create — 生成唯一 invoiceNumber（格式：INV-{YYYYMM}-{6位序號}，例如 INV-202603-000001）
    - Test 2：InvoiceService.create — 同一 treatmentId 已有 issued 發票時拋出 ConflictException
    - Test 3：InvoiceService.issue — draft → issued，設定 issuedAt = new Date()
    - Test 4：InvoiceService.issue — 對 cancelled 發票調用 issue 時拋出 BadRequestException
    - Test 5：InvoiceService.cancel — issued/draft → cancelled，設定 cancelledAt
    - Test 6：InvoiceService.create — lineItems 從 FeeCalculationService.calculateBalance().payments 自動生成
  </behavior>

  <action>
建立 `backend/src/revenue/entities/invoice.entity.ts`：

```typescript
@Entity('invoices')
export class Invoice {
  @PrimaryGeneratedColumn('uuid')
  id: string;

  // 唯一發票號碼，格式：INV-{YYYYMM}-{6位序號}
  @Column({ type: 'varchar', length: 30, unique: true })
  invoiceNumber: string;

  @Column({ type: 'varchar', length: 32 })
  treatmentId: string;

  @ManyToOne(() => Treatment)
  @JoinColumn({ name: 'treatmentId' })
  treatment?: Treatment;

  @Column({ type: 'varchar', length: 32 })
  patientId: string;

  @ManyToOne(() => Patient)
  @JoinColumn({ name: 'patientId' })
  patient?: Patient;

  // 費用明細（JSON 陣列）
  // 格式：[{ paymentId, amount, paymentMethod, paidAt, description }]
  @Column({ type: 'json' })
  lineItems: InvoiceLineItem[];

  @Column({ type: 'decimal', precision: 20, scale: 2 })
  totalAmount: number;

  // 發票狀態：draft（草稿）| issued（已開立）| cancelled（已取消）
  @Column({ type: 'varchar', length: 20, default: 'draft' })
  status: 'draft' | 'issued' | 'cancelled';

  @Column({ type: 'datetime', nullable: true })
  issuedAt: Date | null;

  @Column({ type: 'datetime', nullable: true })
  cancelledAt: Date | null;

  @Column({ type: 'varchar', length: 32, nullable: true })
  cancelReason: string | null;

  @Column({ type: 'varchar', length: 32 })
  clinicId: string;

  @Column({ type: 'varchar', length: 32, nullable: true })
  createdBy: string | null;

  @CreateDateColumn()
  createdAt: Date;

  @UpdateDateColumn()
  updatedAt: Date;

  @VersionColumn()
  version: number;
}

export interface InvoiceLineItem {
  paymentId: string;
  amount: number;
  paymentMethod: 'cash' | 'bank_transfer' | 'credit_card';
  paidAt: string; // ISO 8601
  description: string; // 例如：「療程費用（現金）」
}
```

建立 `backend/src/revenue/dto/create-invoice.dto.ts`：

```typescript
export class CreateInvoiceDto {
  @IsString() @IsNotEmpty() treatmentId: string;
  @IsString() @IsNotEmpty() patientId: string;
  @IsOptional() @IsString() createdBy?: string;
  // lineItems 和 totalAmount 由服務自動生成，不接受外部傳入
}
```

建立 `backend/src/revenue/services/invoice.service.ts`：

```typescript
@Injectable()
export class InvoiceService {
  private readonly logger = new Logger(InvoiceService.name);

  constructor(
    @InjectRepository(Invoice) private invoiceRepo: Repository<Invoice>,
    @InjectRepository(Treatment) private treatmentRepo: Repository<Treatment>,
    @InjectRepository(Patient) private patientRepo: Repository<Patient>,
    private feeCalculationService: FeeCalculationService,
  ) {}

  /**
   * 生成發票（FIN-04）
   * 1. 確認無重複 issued 發票
   * 2. 從 FeeCalculationService 取得付款明細
   * 3. 生成唯一 invoiceNumber（INV-{YYYYMM}-{6位序號}）
   * 4. 儲存 draft 發票
   */
  async create(dto: CreateInvoiceDto, clinicId: string): Promise<Invoice> {
    // 檢查重複
    const existingIssued = await this.invoiceRepo.findOne({
      where: { treatmentId: dto.treatmentId, clinicId, status: 'issued' }
    });
    if (existingIssued) {
      throw new ConflictException(`療程 ${dto.treatmentId} 已存在已開立發票 ${existingIssued.invoiceNumber}`);
    }

    // 取得費用明細
    const balanceInfo = await this.feeCalculationService.calculateBalance(
      dto.treatmentId, clinicId
    );

    // 生成 lineItems
    const lineItems: InvoiceLineItem[] = balanceInfo.payments.map(p => ({
      paymentId: p.id,
      amount: p.amount,
      paymentMethod: p.paymentMethod,
      paidAt: p.paidAt.toISOString(),
      description: `療程費用（${this.getPaymentMethodLabel(p.paymentMethod)}）`,
    }));

    // 生成唯一流水號
    const invoiceNumber = await this.generateInvoiceNumber(clinicId);

    const invoice = this.invoiceRepo.create({
      invoiceNumber,
      treatmentId: dto.treatmentId,
      patientId: dto.patientId,
      lineItems,
      totalAmount: balanceInfo.totalPaid, // 已付總額
      status: 'draft',
      clinicId,
      createdBy: dto.createdBy ?? null,
      issuedAt: null,
      cancelledAt: null,
      cancelReason: null,
    });

    return await this.invoiceRepo.save(invoice);
  }

  /** 發票從 draft 轉為 issued（FIN-04）*/
  async issue(id: string, clinicId: string): Promise<Invoice> {
    const invoice = await this.findOne(id, clinicId);
    if (invoice.status !== 'draft') {
      throw new BadRequestException(`發票狀態 ${invoice.status} 無法開立，只有 draft 可開立`);
    }
    invoice.status = 'issued';
    invoice.issuedAt = new Date();
    return await this.invoiceRepo.save(invoice);
  }

  /** 取消發票 */
  async cancel(id: string, clinicId: string, reason?: string): Promise<Invoice> {
    const invoice = await this.findOne(id, clinicId);
    if (invoice.status === 'cancelled') {
      throw new BadRequestException('發票已取消');
    }
    invoice.status = 'cancelled';
    invoice.cancelledAt = new Date();
    invoice.cancelReason = reason ?? null;
    return await this.invoiceRepo.save(invoice);
  }

  async findOne(id: string, clinicId: string): Promise<Invoice> {
    const invoice = await this.invoiceRepo.findOne({
      where: { id, clinicId },
      relations: ['treatment', 'patient'],
    });
    if (!invoice) throw new NotFoundException(`Invoice ${id} not found`);
    return invoice;
  }

  async findByTreatment(treatmentId: string, clinicId: string): Promise<Invoice[]> {
    return this.invoiceRepo.find({
      where: { treatmentId, clinicId },
      order: { createdAt: 'DESC' },
    });
  }

  async findByPatient(patientId: string, clinicId: string): Promise<Invoice[]> {
    return this.invoiceRepo.find({
      where: { patientId, clinicId },
      order: { createdAt: 'DESC' },
      relations: ['treatment'],
    });
  }

  private async generateInvoiceNumber(clinicId: string): Promise<string> {
    const now = new Date();
    const yyyymm = `${now.getFullYear()}${String(now.getMonth() + 1).padStart(2, '0')}`;
    const prefix = `INV-${yyyymm}-`;

    // 查詢本月最大序號
    const latest = await this.invoiceRepo
      .createQueryBuilder('invoice')
      .where('invoice.clinicId = :clinicId', { clinicId })
      .andWhere('invoice.invoiceNumber LIKE :prefix', { prefix: `${prefix}%` })
      .orderBy('invoice.invoiceNumber', 'DESC')
      .getOne();

    let seq = 1;
    if (latest) {
      const lastSeq = parseInt(latest.invoiceNumber.split('-').pop() ?? '0', 10);
      seq = lastSeq + 1;
    }

    return `${prefix}${String(seq).padStart(6, '0')}`;
  }

  private getPaymentMethodLabel(method: string): string {
    const labels: Record<string, string> = {
      cash: '現金',
      bank_transfer: '銀行轉帳',
      credit_card: '刷卡',
    };
    return labels[method] ?? method;
  }
}
```

測試 `invoice.service.spec.ts`：覆蓋全部 6 個 behavior 測試，mock Repository + FeeCalculationService。
  </action>

  <verify>
    <automated>cd /home/hsu/Desktop/doctor-crm/backend && npx jest invoice.service.spec.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "invoiceNumber\|lineItems\|issuedAt\|cancelledAt" backend/src/revenue/entities/invoice.entity.ts → ≥4
    - grep -c "InvoiceLineItem" backend/src/revenue/entities/invoice.entity.ts → ≥1
    - grep -c "ConflictException\|BadRequestException" backend/src/revenue/services/invoice.service.ts → ≥2
    - grep -c "INV-\|invoiceNumber" backend/src/revenue/services/invoice.service.ts → ≥2
    - npx jest invoice.service.spec.ts → 6 tests passing
  </acceptance_criteria>

  <done>Invoice 實體建立，InvoiceService 實現 create/issue/cancel/findOne/findByPatient/findByTreatment，invoiceNumber 格式 INV-{YYYYMM}-{6位序號}，重複開立防護正常，6 項測試通過。</done>
</task>

<task type="auto">
  <name>Task 2：InvoiceController 與 RevenueModule 更新</name>
  <files>
    backend/src/revenue/controllers/invoice.controller.ts
    backend/src/revenue/revenue.module.ts
  </files>

  <read_first>
    - backend/src/revenue/services/invoice.service.ts （Task 1 建立，確認方法簽名）
    - backend/src/revenue/controllers/payment.controller.ts （Plan 01 建立，Controller 慣例）
    - backend/src/revenue/revenue.module.ts （現有 providers/controllers，確認加入位置）
    - backend/src/auth/guards/jwt-auth.guard.ts （guard 路徑）
    - backend/src/revenue/entities/invoice.entity.ts （Task 1 建立）
  </read_first>

  <action>
建立 `backend/src/revenue/controllers/invoice.controller.ts`：

```typescript
@UseGuards(JwtAuthGuard)
@Controller('invoices')
export class InvoiceController {
  constructor(private readonly invoiceService: InvoiceService) {}

  /** POST /invoices — 生成草稿發票（FIN-04） */
  @Post()
  create(@Body() dto: CreateInvoiceDto, @Request() req) {
    return this.invoiceService.create(dto, req.user.clinicId);
  }

  /** GET /invoices/:id — 查詢發票詳情（FIN-03） */
  @Get(':id')
  findOne(@Param('id') id: string, @Request() req) {
    return this.invoiceService.findOne(id, req.user.clinicId);
  }

  /** GET /invoices/treatment/:treatmentId — 療程所有發票 */
  @Get('treatment/:treatmentId')
  findByTreatment(@Param('treatmentId') id: string, @Request() req) {
    return this.invoiceService.findByTreatment(id, req.user.clinicId);
  }

  /** GET /invoices/patient/:patientId — 患者所有發票 */
  @Get('patient/:patientId')
  findByPatient(@Param('patientId') id: string, @Request() req) {
    return this.invoiceService.findByPatient(id, req.user.clinicId);
  }

  /** PATCH /invoices/:id/issue — 確認開立發票（draft → issued） */
  @Patch(':id/issue')
  issue(@Param('id') id: string, @Request() req) {
    return this.invoiceService.issue(id, req.user.clinicId);
  }

  /** PATCH /invoices/:id/cancel — 取消發票 */
  @Patch(':id/cancel')
  cancel(
    @Param('id') id: string,
    @Body('reason') reason: string,
    @Request() req,
  ) {
    return this.invoiceService.cancel(id, req.user.clinicId, reason);
  }
}
```

更新 `backend/src/revenue/revenue.module.ts`：
- TypeOrmModule.forFeature([...]) 加入 Invoice、Patient
- providers 加入 InvoiceService
- controllers 加入 InvoiceController
- exports 加入 InvoiceService
  </action>

  <verify>
    <automated>cd /home/hsu/Desktop/doctor-crm/backend && npm run build 2>&1 | grep -c "error TS"</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "InvoiceController\|InvoiceService" backend/src/revenue/revenue.module.ts → ≥2
    - grep -c "Invoice" backend/src/revenue/revenue.module.ts → ≥3
    - grep -c "@Get\|@Post\|@Patch" backend/src/revenue/controllers/invoice.controller.ts → ≥5
    - grep -c "req.user.clinicId" backend/src/revenue/controllers/invoice.controller.ts → ≥4
    - cd backend && npm run build 2>&1 | grep "error TS" | wc -l → 0
  </acceptance_criteria>

  <done>InvoiceController 提供 6 個 REST 端點（POST/GET×3/PATCH×2），JwtAuthGuard 保護。RevenueModule 正確匯入並導出 InvoiceService。npm run build 零錯誤。</done>
</task>

</tasks>

<verification>
完整驗證：
```bash
cd /home/hsu/Desktop/doctor-crm/backend
npx jest invoice.service.spec.ts --no-coverage
npm run build
```

期望：6 項測試通過，TypeScript 編譯零錯誤。
</verification>

<success_criteria>
- Invoice 實體含 invoiceNumber、lineItems（JSON）、totalAmount、status（draft/issued/cancelled）、issuedAt、cancelledAt、clinicId
- InvoiceService 防止重複開立（ConflictException）
- invoiceNumber 自動生成，格式 INV-{YYYYMM}-{6位序號}
- 6 個 REST API 端點，全部 JwtAuthGuard 保護
- npm run build 零 TypeScript 錯誤
</success_criteria>

<output>
完成後建立 `.planning/phases/03-financial-management-enhancement/03-02-SUMMARY.md`
</output>
