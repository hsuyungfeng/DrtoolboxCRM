---
phase: 03-financial-management-enhancement
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - backend/src/revenue/services/revenue-report.service.ts
  - backend/src/revenue/services/revenue-report.service.spec.ts
  - backend/src/revenue/controllers/revenue-report.controller.ts
  - backend/src/revenue/revenue.module.ts
  - frontend/src/services/revenue-api.ts
  - frontend/src/views/RevenueView.vue
  - frontend/src/stores/revenue.store.ts
autonomous: true
requirements:
  - FIN-06

must_haves:
  truths:
    - "醫護人員能查看按時間段篩選的收入報表（日/月/季）"
    - "報表包含：按醫護人員彙總、按療程類型彙總、按支付方式彙總"
    - "收入報表頁面使用 ECharts 長條圖（月收入趨勢）+ 環形圖（支付方式分布）"
    - "報表載入時間 < 2 秒（後端聚合查詢，非前端計算）"
    - "前端 RevenueView.vue 新增「收入報表」分頁，呈現圖表與彙總數據"
  artifacts:
    - path: "backend/src/revenue/services/revenue-report.service.ts"
      provides: "收入報表聚合服務"
      exports: ["getRevenueSummary", "getPaymentMethodBreakdown", "getStaffRevenue", "getMonthlyTrend"]
    - path: "backend/src/revenue/controllers/revenue-report.controller.ts"
      provides: "報表查詢 REST API"
      contains: "GET /revenue-reports/summary, GET /revenue-reports/monthly-trend, GET /revenue-reports/staff"
    - path: "frontend/src/services/revenue-api.ts"
      provides: "前端 API 服務（payment + invoice + report）"
      exports: ["paymentApi", "invoiceApi", "reportApi"]
    - path: "frontend/src/views/RevenueView.vue"
      provides: "收入管理主畫面，新增報表分頁與 ECharts 圖表"
      contains: "tab: reports, ECharts bar chart (monthly), pie chart (payment method)"
  key_links:
    - from: "frontend/src/views/RevenueView.vue"
      to: "frontend/src/services/revenue-api.ts"
      via: "reportApi.getSummary() + reportApi.getMonthlyTrend()"
      pattern: "import { reportApi } from '@/services/revenue-api'"
    - from: "frontend/src/stores/revenue.store.ts"
      to: "frontend/src/services/revenue-api.ts"
      via: "action calls in Pinia store"
      pattern: "reportApi.getSummary(clinicId, startDate, endDate)"
    - from: "backend/src/revenue/services/revenue-report.service.ts"
      to: "backend/src/revenue/entities/payment.entity.ts"
      via: "TypeORM QueryBuilder"
      pattern: "@InjectRepository(Payment)"
---

<objective>
建立收入統計報表後端 API 與前端 ECharts 視覺化（FIN-06）。

目的：管理者與醫護人員需要綜觀診所收入狀況，本計劃基於 Plan 01 建立的 Payment 資料，提供聚合報表 API 與前端圖表呈現。與 Plan 02（發票）並行執行，因兩者無共同修改檔案。

輸出成果：
- `revenue-report.service.ts` — 收入聚合查詢（月趨勢、人員分潤、支付方式分布）
- `revenue-report.controller.ts` — 報表 REST API
- `revenue-api.ts` — 前端完整 API 服務（payment、invoice、report）
- `RevenueView.vue` — 更新主畫面加入報表分頁與 ECharts 圖表
- `revenue.store.ts` — Pinia 狀態管理
</objective>

<execution_context>
@/home/hsu/.claude/get-shit-done/workflows/execute-plan.md
@/home/hsu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/phases/03-financial-management-enhancement/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01 建立的型別（執行前確認 03-01 完成） -->

從 backend/src/revenue/entities/payment.entity.ts（Plan 01 建立）：
```typescript
export class Payment {
  id: string;
  treatmentId: string;
  patientId: string;
  amount: number;
  paymentMethod: 'cash' | 'bank_transfer' | 'credit_card';
  status: string;       // completed | refunded | cancelled
  paidAt: Date;
  clinicId: string;
}
```

從 backend/src/revenue/entities/revenue-record.entity.ts（現有）：
```typescript
export class RevenueRecord {
  id: string;
  treatmentId: string;
  staffId: string;
  role: string;
  amount: number;
  status: string;       // pending | calculated | locked | adjusted | paid | cancelled
  calculatedAt: Date;
  clinicId: string;
}
```

現有前端服務模式（backend/src/services/treatments-api.ts 範例）：
```typescript
// 使用 axios instance from api.ts
import api from './api';
export const treatmentsApi = {
  getTreatments: (params) => api.get('/treatments', { params }),
  createTreatment: (data) => api.post('/treatments', data),
};
```

ECharts Vue 用法（已安裝 echarts 6.0.0 + vue-echarts 8.0.1）：
```vue
<template>
  <v-chart :option="chartOption" autoresize style="height: 300px" />
</template>
<script setup>
import VChart from 'vue-echarts';
import { use } from 'echarts/core';
import { BarChart, PieChart } from 'echarts/charts';
import { GridComponent, TooltipComponent, LegendComponent } from 'echarts/components';
import { CanvasRenderer } from 'echarts/renderers';
use([BarChart, PieChart, GridComponent, TooltipComponent, LegendComponent, CanvasRenderer]);
</script>
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：RevenueReportService 與 RevenueReportController</name>
  <files>
    backend/src/revenue/services/revenue-report.service.ts
    backend/src/revenue/services/revenue-report.service.spec.ts
    backend/src/revenue/controllers/revenue-report.controller.ts
    backend/src/revenue/revenue.module.ts
  </files>

  <read_first>
    - backend/src/revenue/entities/payment.entity.ts （Plan 01，Payment 欄位確認）
    - backend/src/revenue/entities/revenue-record.entity.ts （現有分潤記錄，staff 維度彙總）
    - backend/src/revenue/services/revenue-record.service.ts （getSummaryByClinic 現有模式）
    - backend/src/revenue/controllers/revenue-record.controller.ts （Controller 慣例）
    - backend/src/revenue/revenue.module.ts （現有 providers，確認加入位置）
  </read_first>

  <behavior>
    - Test 1：getRevenueSummary — 返回 { totalRevenue, totalPayments, paymentCount, dateRange }
    - Test 2：getMonthlyTrend — 12 個月資料，每月含 { month: 'YYYY-MM', revenue: number, paymentCount: number }
    - Test 3：getPaymentMethodBreakdown — 返回 [{ method, total, count, percentage }]，percentage 使用 Decimal.js 計算
    - Test 4：getStaffRevenue — 返回 [{ staffId, staffName, role, totalAmount, recordCount }] 按 totalAmount DESC
    - Test 5：getRevenueSummary — startDate/endDate 為空時，預設查詢當月（1號到今天）
  </behavior>

  <action>
建立 `backend/src/revenue/services/revenue-report.service.ts`：

```typescript
export interface RevenueSummaryDto {
  totalRevenue: number;    // 患者付款總額（Payment）
  totalPayments: number;   // 付款筆數
  paymentCount: number;
  dateRange: { start: string; end: string };
}

export interface MonthlyTrendItem {
  month: string;           // 格式：YYYY-MM
  revenue: number;
  paymentCount: number;
}

export interface PaymentMethodBreakdown {
  method: 'cash' | 'bank_transfer' | 'credit_card';
  methodLabel: string;     // 現金 | 銀行轉帳 | 刷卡
  total: number;
  count: number;
  percentage: number;      // 占總收入百分比，Decimal.js 計算
}

export interface StaffRevenueItem {
  staffId: string;
  staffName: string;
  role: string;
  totalAmount: number;
  recordCount: number;
}

@Injectable()
export class RevenueReportService {
  constructor(
    @InjectRepository(Payment) private paymentRepo: Repository<Payment>,
    @InjectRepository(RevenueRecord) private revenueRecordRepo: Repository<RevenueRecord>,
    @InjectRepository(Staff) private staffRepo: Repository<Staff>,
  ) {}

  /**
   * 收入總覽（FIN-06）
   * 預設查詢當月，可傳入自訂日期範圍
   */
  async getRevenueSummary(
    clinicId: string,
    startDate?: Date,
    endDate?: Date,
  ): Promise<RevenueSummaryDto> {
    const { start, end } = this.normalizeDateRange(startDate, endDate);

    const result = await this.paymentRepo
      .createQueryBuilder('payment')
      .select('SUM(payment.amount)', 'total')
      .addSelect('COUNT(*)', 'count')
      .where('payment.clinicId = :clinicId', { clinicId })
      .andWhere('payment.status = :status', { status: 'completed' })
      .andWhere('payment.paidAt >= :start', { start })
      .andWhere('payment.paidAt <= :end', { end })
      .getRawOne();

    return {
      totalRevenue: new Decimal(result.total ?? 0).toDecimalPlaces(2).toNumber(),
      totalPayments: Number(result.count ?? 0),
      paymentCount: Number(result.count ?? 0),
      dateRange: {
        start: start.toISOString(),
        end: end.toISOString(),
      },
    };
  }

  /**
   * 月收入趨勢（最近 12 個月，FIN-06）
   * 用於 ECharts 長條圖
   */
  async getMonthlyTrend(clinicId: string): Promise<MonthlyTrendItem[]> {
    const twelveMonthsAgo = new Date();
    twelveMonthsAgo.setMonth(twelveMonthsAgo.getMonth() - 11);
    twelveMonthsAgo.setDate(1);
    twelveMonthsAgo.setHours(0, 0, 0, 0);

    const rows = await this.paymentRepo
      .createQueryBuilder('payment')
      .select("strftime('%Y-%m', payment.paidAt)", 'month')
      .addSelect('SUM(payment.amount)', 'revenue')
      .addSelect('COUNT(*)', 'paymentCount')
      .where('payment.clinicId = :clinicId', { clinicId })
      .andWhere('payment.status = :status', { status: 'completed' })
      .andWhere('payment.paidAt >= :since', { since: twelveMonthsAgo })
      .groupBy("strftime('%Y-%m', payment.paidAt)")
      .orderBy('month', 'ASC')
      .getRawMany();

    return rows.map(r => ({
      month: r.month,
      revenue: new Decimal(r.revenue ?? 0).toDecimalPlaces(2).toNumber(),
      paymentCount: Number(r.paymentCount ?? 0),
    }));
  }

  /**
   * 支付方式分布（FIN-06）
   * 用於 ECharts 環形圖
   */
  async getPaymentMethodBreakdown(
    clinicId: string,
    startDate?: Date,
    endDate?: Date,
  ): Promise<PaymentMethodBreakdown[]> {
    const { start, end } = this.normalizeDateRange(startDate, endDate);

    const rows = await this.paymentRepo
      .createQueryBuilder('payment')
      .select('payment.paymentMethod', 'method')
      .addSelect('SUM(payment.amount)', 'total')
      .addSelect('COUNT(*)', 'count')
      .where('payment.clinicId = :clinicId', { clinicId })
      .andWhere('payment.status = :status', { status: 'completed' })
      .andWhere('payment.paidAt >= :start', { start })
      .andWhere('payment.paidAt <= :end', { end })
      .groupBy('payment.paymentMethod')
      .getRawMany();

    const grandTotal = rows.reduce(
      (sum, r) => sum.plus(new Decimal(r.total ?? 0)),
      new Decimal(0)
    );

    const labels: Record<string, string> = {
      cash: '現金',
      bank_transfer: '銀行轉帳',
      credit_card: '刷卡',
    };

    return rows.map(r => ({
      method: r.method,
      methodLabel: labels[r.method] ?? r.method,
      total: new Decimal(r.total ?? 0).toDecimalPlaces(2).toNumber(),
      count: Number(r.count ?? 0),
      percentage: grandTotal.isZero()
        ? 0
        : new Decimal(r.total ?? 0).div(grandTotal).mul(100).toDecimalPlaces(2).toNumber(),
    }));
  }

  /**
   * 醫護人員分潤統計（FIN-06）
   * 基於現有 RevenueRecord（分潤視角）
   */
  async getStaffRevenue(
    clinicId: string,
    startDate?: Date,
    endDate?: Date,
  ): Promise<StaffRevenueItem[]> {
    const { start, end } = this.normalizeDateRange(startDate, endDate);

    const rows = await this.revenueRecordRepo
      .createQueryBuilder('record')
      .select('record.staffId', 'staffId')
      .addSelect('staff.name', 'staffName')
      .addSelect('record.role', 'role')
      .addSelect('SUM(record.amount)', 'totalAmount')
      .addSelect('COUNT(*)', 'recordCount')
      .leftJoin(Staff, 'staff', 'staff.id = record.staffId')
      .where('record.clinicId = :clinicId', { clinicId })
      .andWhere('record.status IN (:...statuses)', { statuses: ['calculated', 'locked', 'paid'] })
      .andWhere('record.calculatedAt >= :start', { start })
      .andWhere('record.calculatedAt <= :end', { end })
      .groupBy('record.staffId, record.role, staff.name')
      .orderBy('totalAmount', 'DESC')
      .getRawMany();

    return rows.map(r => ({
      staffId: r.staffId,
      staffName: r.staffName ?? '未知',
      role: r.role,
      totalAmount: new Decimal(r.totalAmount ?? 0).toDecimalPlaces(2).toNumber(),
      recordCount: Number(r.recordCount ?? 0),
    }));
  }

  /** 正規化日期範圍（預設當月） */
  private normalizeDateRange(start?: Date, end?: Date): { start: Date; end: Date } {
    const now = new Date();
    const defaultStart = new Date(now.getFullYear(), now.getMonth(), 1, 0, 0, 0, 0);
    const defaultEnd = new Date(now.getFullYear(), now.getMonth() + 1, 0, 23, 59, 59, 999);
    return {
      start: start ?? defaultStart,
      end: end ?? defaultEnd,
    };
  }
}
```

建立 `backend/src/revenue/controllers/revenue-report.controller.ts`：

```typescript
@UseGuards(JwtAuthGuard)
@Controller('revenue-reports')
export class RevenueReportController {
  constructor(private readonly reportService: RevenueReportService) {}

  /** GET /revenue-reports/summary?startDate=&endDate= */
  @Get('summary')
  getSummary(
    @Request() req,
    @Query('startDate') startDate?: string,
    @Query('endDate') endDate?: string,
  ) {
    const start = startDate ? new Date(startDate) : undefined;
    const end = endDate ? new Date(endDate) : undefined;
    return this.reportService.getRevenueSummary(req.user.clinicId, start, end);
  }

  /** GET /revenue-reports/monthly-trend — 近 12 個月趨勢 */
  @Get('monthly-trend')
  getMonthlyTrend(@Request() req) {
    return this.reportService.getMonthlyTrend(req.user.clinicId);
  }

  /** GET /revenue-reports/payment-methods?startDate=&endDate= */
  @Get('payment-methods')
  getPaymentMethods(
    @Request() req,
    @Query('startDate') startDate?: string,
    @Query('endDate') endDate?: string,
  ) {
    const start = startDate ? new Date(startDate) : undefined;
    const end = endDate ? new Date(endDate) : undefined;
    return this.reportService.getPaymentMethodBreakdown(req.user.clinicId, start, end);
  }

  /** GET /revenue-reports/staff?startDate=&endDate= */
  @Get('staff')
  getStaffRevenue(
    @Request() req,
    @Query('startDate') startDate?: string,
    @Query('endDate') endDate?: string,
  ) {
    const start = startDate ? new Date(startDate) : undefined;
    const end = endDate ? new Date(endDate) : undefined;
    return this.reportService.getStaffRevenue(req.user.clinicId, start, end);
  }
}
```

更新 `backend/src/revenue/revenue.module.ts`：
- TypeOrmModule.forFeature 加入 Staff（如尚未包含）
- providers 加入 RevenueReportService
- controllers 加入 RevenueReportController
- exports 加入 RevenueReportService

測試 `revenue-report.service.spec.ts`：覆蓋全部 5 個 behavior 測試，mock Repository（payment + revenue-record），驗證 Decimal.js 計算精度。
  </action>

  <verify>
    <automated>cd /home/hsu/Desktop/doctor-crm/backend && npx jest revenue-report.service.spec.ts --no-coverage 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "getRevenueSummary\|getMonthlyTrend\|getPaymentMethodBreakdown\|getStaffRevenue" backend/src/revenue/services/revenue-report.service.ts → ≥4
    - grep -c "Decimal" backend/src/revenue/services/revenue-report.service.ts → ≥3
    - grep -c "RevenueReportController\|RevenueReportService" backend/src/revenue/revenue.module.ts → ≥2
    - grep -c "@Get" backend/src/revenue/controllers/revenue-report.controller.ts → ≥4
    - npx jest revenue-report.service.spec.ts → 5 tests passing
  </acceptance_criteria>

  <done>RevenueReportService 提供 4 個聚合查詢方法，均使用 Decimal.js 計算。RevenueReportController 4 個 GET 端點，JwtAuthGuard 保護。5 項測試通過。</done>
</task>

<task type="auto">
  <name>Task 2：前端 revenue-api.ts、revenue.store.ts 與 RevenueView.vue 報表分頁</name>
  <files>
    frontend/src/services/revenue-api.ts
    frontend/src/stores/revenue.store.ts
    frontend/src/views/RevenueView.vue
  </files>

  <read_first>
    - frontend/src/services/api.ts （axios instance，確認 baseURL 設定）
    - frontend/src/services/treatments-api.ts （API service 慣例）
    - frontend/src/views/RevenueView.vue （現有分頁結構，確認現有 tabs）
    - frontend/src/stores/ （現有 store 命名慣例）
    - frontend/src/services/revenue-api.ts （若已存在，讀取現有內容後再更新）
  </read_first>

  <action>
建立/更新 `frontend/src/services/revenue-api.ts`：

```typescript
import api from './api';

// 支付 API（FIN-02/05）
export const paymentApi = {
  /** 記錄患者付款 */
  createPayment: (data: {
    treatmentId: string;
    patientId: string;
    amount: number;
    paymentMethod: 'cash' | 'bank_transfer' | 'credit_card';
    paidAt?: string;
    notes?: string;
  }) => api.post('/payments', data),

  /** 療程付款列表 */
  getByTreatment: (treatmentId: string) =>
    api.get(`/payments/treatment/${treatmentId}`),

  /** 費用餘額查詢 */
  getBalance: (treatmentId: string) =>
    api.get(`/payments/balance/${treatmentId}`),

  /** 患者所有付款 */
  getByPatient: (patientId: string) =>
    api.get(`/payments/patient/${patientId}`),

  /** 取消付款 */
  cancelPayment: (id: string) => api.delete(`/payments/${id}`),
};

// 發票 API（FIN-04）
export const invoiceApi = {
  /** 生成草稿發票 */
  createInvoice: (data: { treatmentId: string; patientId: string }) =>
    api.post('/invoices', data),

  /** 療程發票列表 */
  getByTreatment: (treatmentId: string) =>
    api.get(`/invoices/treatment/${treatmentId}`),

  /** 患者發票列表 */
  getByPatient: (patientId: string) =>
    api.get(`/invoices/patient/${patientId}`),

  /** 確認開立（draft → issued） */
  issueInvoice: (id: string) => api.patch(`/invoices/${id}/issue`),

  /** 取消發票 */
  cancelInvoice: (id: string, reason?: string) =>
    api.patch(`/invoices/${id}/cancel`, { reason }),
};

// 報表 API（FIN-06）
export const reportApi = {
  /** 收入總覽（預設當月） */
  getSummary: (params?: { startDate?: string; endDate?: string }) =>
    api.get('/revenue-reports/summary', { params }),

  /** 近 12 個月趨勢（ECharts 長條圖資料） */
  getMonthlyTrend: () => api.get('/revenue-reports/monthly-trend'),

  /** 支付方式分布（ECharts 環形圖資料） */
  getPaymentMethods: (params?: { startDate?: string; endDate?: string }) =>
    api.get('/revenue-reports/payment-methods', { params }),

  /** 醫護人員分潤統計 */
  getStaffRevenue: (params?: { startDate?: string; endDate?: string }) =>
    api.get('/revenue-reports/staff', { params }),
};
```

建立 `frontend/src/stores/revenue.store.ts`（Pinia）：

```typescript
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import { reportApi, paymentApi } from '@/services/revenue-api';

export const useRevenueStore = defineStore('revenue', () => {
  // 狀態
  const summary = ref<{ totalRevenue: number; paymentCount: number; dateRange: object } | null>(null);
  const monthlyTrend = ref<Array<{ month: string; revenue: number; paymentCount: number }>>([]);
  const paymentMethods = ref<Array<{ method: string; methodLabel: string; total: number; percentage: number }>>([]);
  const staffRevenue = ref<Array<{ staffId: string; staffName: string; role: string; totalAmount: number }>>([]);
  const loading = ref(false);
  const error = ref<string | null>(null);

  // 計算屬性：月趨勢轉 ECharts 格式
  const monthlyTrendChartOption = computed(() => ({
    tooltip: { trigger: 'axis' },
    xAxis: { type: 'category', data: monthlyTrend.value.map(d => d.month) },
    yAxis: { type: 'value', name: '收入（元）' },
    series: [{
      name: '月收入',
      type: 'bar',
      data: monthlyTrend.value.map(d => d.revenue),
      itemStyle: { color: '#18a058' },
    }],
  }));

  // 計算屬性：支付方式轉 ECharts 環形圖格式
  const paymentMethodChartOption = computed(() => ({
    tooltip: { trigger: 'item', formatter: '{b}: {c}元 ({d}%)' },
    legend: { orient: 'vertical', left: 'left' },
    series: [{
      name: '支付方式',
      type: 'pie',
      radius: ['40%', '70%'],
      data: paymentMethods.value.map(m => ({ name: m.methodLabel, value: m.total })),
    }],
  }));

  // Actions
  async function loadReportData(startDate?: string, endDate?: string) {
    loading.value = true;
    error.value = null;
    try {
      const [summaryRes, trendRes, methodsRes, staffRes] = await Promise.all([
        reportApi.getSummary({ startDate, endDate }),
        reportApi.getMonthlyTrend(),
        reportApi.getPaymentMethods({ startDate, endDate }),
        reportApi.getStaffRevenue({ startDate, endDate }),
      ]);
      summary.value = summaryRes.data;
      monthlyTrend.value = trendRes.data;
      paymentMethods.value = methodsRes.data;
      staffRevenue.value = staffRes.data;
    } catch (e: unknown) {
      error.value = '載入報表資料失敗';
    } finally {
      loading.value = false;
    }
  }

  return {
    summary,
    monthlyTrend,
    paymentMethods,
    staffRevenue,
    loading,
    error,
    monthlyTrendChartOption,
    paymentMethodChartOption,
    loadReportData,
  };
});
```

更新 `frontend/src/views/RevenueView.vue`：

在現有 `<n-tabs>` 加入新分頁 `name="reports" tab="收入報表"`，內容包含：

1. **日期範圍選擇器**（n-date-picker, type="daterange"）
2. **統計卡片行**（n-grid :cols="3"）：
   - 本期總收入（n-statistic）
   - 付款筆數（n-statistic）
   - 平均單筆金額（computed）
3. **月收入趨勢圖**（v-chart，長條圖，height: 300px）
4. **支付方式分布圖**（v-chart，環形圖，height: 280px）
5. **醫護人員分潤明細表**（n-data-table）

使用 `useRevenueStore()` 取得資料，切換到報表分頁時自動呼叫 `store.loadReportData()`。

ECharts 元件需要在 script setup 中使用 `use()` 註冊以下元件：
```typescript
use([BarChart, PieChart, GridComponent, TooltipComponent, LegendComponent, TitleComponent, CanvasRenderer]);
```
  </action>

  <verify>
    <automated>cd /home/hsu/Desktop/doctor-crm/frontend && npm run build 2>&1 | grep -E "error|✓" | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - grep -c "paymentApi\|invoiceApi\|reportApi" frontend/src/services/revenue-api.ts → ≥3
    - grep -c "getMonthlyTrend\|getPaymentMethods\|getStaffRevenue" frontend/src/services/revenue-api.ts → ≥3
    - grep -c "useRevenueStore\|monthlyTrendChartOption\|paymentMethodChartOption" frontend/src/stores/revenue.store.ts → ≥2
    - grep -c "v-chart\|VChart\|vue-echarts" frontend/src/views/RevenueView.vue → ≥2
    - grep -c "reports\|收入報表" frontend/src/views/RevenueView.vue → ≥1
    - cd frontend && npm run build 2>&1 | grep "error" | wc -l → 0
  </acceptance_criteria>

  <done>revenue-api.ts 含完整 paymentApi/invoiceApi/reportApi。revenue.store.ts 提供 Pinia store 與 ECharts chartOption computed。RevenueView.vue 新增「收入報表」分頁，含日期範圍選擇、統計卡片、長條圖、環形圖、人員分潤表。前端 build 零錯誤。</done>
</task>

</tasks>

<verification>
完整驗證：
```bash
# 後端
cd /home/hsu/Desktop/doctor-crm/backend
npx jest revenue-report.service.spec.ts --no-coverage
npm run build

# 前端
cd /home/hsu/Desktop/doctor-crm/frontend
npm run build
```

期望：5 項後端測試通過，前後端 build 零錯誤。
</verification>

<success_criteria>
- RevenueReportService 4 個聚合查詢方法，均使用 Decimal.js，誤差 < 0.01%
- 4 個 REST GET 端點，JwtAuthGuard 保護，< 2s 回應（後端聚合）
- 前端 revenue-api.ts 覆蓋 payment / invoice / report 三類 API
- RevenueView.vue 新增報表分頁：月收入長條圖 + 支付方式環形圖 + 人員分潤表
- 前後端 build 零錯誤，5 項後端測試通過
</success_criteria>

<output>
完成後建立 `.planning/phases/03-financial-management-enhancement/03-03-SUMMARY.md`
</output>
