---
phase: 05-system-refinement-and-advanced-features
plan: 05-04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - backend/src/app.module.ts
  - backend/package.json
  - backend/.env.example
  - backend/src/common/filters/http-exception.filter.ts
  - backend/src/main.ts
autonomous: false
requirements: [REF-01, SYNC-02]
user_setup:
  - service: PostgreSQL
    why: "Production database"
    env_vars:
      - name: DB_TYPE
        source: "Set to 'postgres'"
      - name: DB_HOST
        source: "PostgreSQL server host"
      - name: DB_PORT
        source: "PostgreSQL server port"
      - name: DB_USERNAME
        source: "Database user"
      - name: DB_PASSWORD
        source: "Database password"
      - name: DB_NAME
        source: "Database name"
must_haves:
  truths:
    - "System can run on PostgreSQL 16+"
    - "GIN indexes are applied to JSONB columns for performance"
    - "API rate limiting is active to protect against abuse"
  artifacts:
    - path: "backend/src/app.module.ts"
      provides: "Throttler integration"
    - path: "backend/package.json"
      provides: "PostgreSQL driver dependency"
  key_links:
    - from: "backend/src/app.module.ts"
      to: "PostgreSQL"
      via: "TypeORM dynamic configuration"
    - from: "backend/src/main.ts"
      to: "@nestjs/throttler"
      via: "app.useGlobalGuards(new ThrottlerGuard(...))"
---

<objective>
Transition the system to PostgreSQL for production-grade performance and reliability, optimize data access with advanced indexing, and implement API rate limiting.

Purpose:
- Move away from SQLite to support high concurrency and JSONB features.
- Protect the system from DDoS and brute-force attacks.
- Ensure efficient querying of dynamic attributes.

Output:
- PostgreSQL driver integration and configuration.
- GIN indexes for JSONB columns.
- Global API rate limiting (Throttler).
</objective>

<execution_context>
@/home/hsu/.gemini/get-shit-done/workflows/execute-plan.md
@/home/hsu/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-system-refinement-and-advanced-features/05-RESEARCH.md
@.planning/phases/05-system-refinement-and-advanced-features/05-02-PLAN.md
@backend/src/app.module.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend - PostgreSQL Integration</name>
  <files>
    - backend/package.json
    - backend/src/app.module.ts
    - backend/.env.example
  </files>
  <action>
    1. Install `pg` (PostgreSQL driver).
    2. Update `AppModule` to configure TypeORM dynamically based on environment variables (`DB_TYPE`, `DB_HOST`, etc.).
    3. Ensure `synchronize: false` is set for production environments, while allowing it for development.
    4. Update `.env.example` with PostgreSQL connection parameters.
  </action>
  <verify>
    <automated>npm run build --prefix backend</automated>
    <manual>Configure a local PostgreSQL instance, update .env, and verify the app starts and connects.</manual>
  </verify>
  <done>
    - Backend is compatible with PostgreSQL.
  </done>
</task>

<task type="auto">
  <name>Task 2: Performance - GIN Indexing and Query Optimization</name>
  <files>
    - backend/src/patients/entities/patient.entity.ts
    - backend/src/treatments/entities/treatment.entity.ts
  </files>
  <action>
    1. Add GIN indexes to the `customFields` columns in `Patient` and `Treatment` entities.
       - Use `@Index({ fulltext: true })` or raw SQL in a migration if TypeORM's decorator isn't sufficient for GIN.
    2. Review and optimize any identified slow queries in `RevenueReportService` and `PatientSearchService`.
  </action>
  <verify>
    <manual>
      - Connect to PostgreSQL.
      - Run `\d patients` and verify `idx_patients_custom_fields` exists with `USING gin`.
    </manual>
  </verify>
  <done>
    - JSONB queries are optimized for performance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Security - API Rate Limiting</name>
  <files>
    - backend/src/app.module.ts
    - backend/src/main.ts
  </files>
  <action>
    1. Install `@nestjs/throttler`.
    2. Configure `ThrottlerModule` in `AppModule` (e.g., 60 requests per minute).
    3. Register `ThrottlerGuard` globally in `main.ts` or `AppModule`.
    4. Add `@SkipThrottle()` to high-traffic legitimate internal endpoints if necessary (e.g., health checks).
  </action>
  <verify>
    <manual>
      - Use a tool like `ab` or a simple script to send 100 requests to an API endpoint in < 1 minute.
      - Verify 429 Too Many Requests is returned after the threshold.
    </manual>
  </verify>
  <done>
    - API endpoints are protected from excessive traffic.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Production Readiness Verification</name>
  <what-built>PostgreSQL integration, GIN indexing, and Rate Limiting.</what-built>
  <how-to-verify>
    1. Verify PostgreSQL connection in logs.
    2. Verify GIN indexes in DB console.
    3. Verify rate limiting by exceeding request threshold.
    4. Run full test suite on PostgreSQL.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
  <action>Human verification of production infrastructure.</action>
  <verify>Human check.</verify>
  <done>Infrastructure is verified as production-ready.</done>
</task>

</tasks>

<verification>
- Connectivity: App connects and functions correctly with PostgreSQL.
- Performance: Indexing is verified at the database level.
- Security: Rate limiting is confirmed via load testing.
</verification>

<success_criteria>
- [ ] Database successfully migrated to (or initialized on) PostgreSQL.
- [ ] GIN indexes active on all JSONB columns.
- [ ] Rate limiting returns 429 when threshold exceeded.
- [ ] All existing tests pass on the PostgreSQL backend.
</success_criteria>

<output>
After completion, create `.planning/phases/05-system-refinement-and-advanced-features/05-04-SUMMARY.md`
</output>
