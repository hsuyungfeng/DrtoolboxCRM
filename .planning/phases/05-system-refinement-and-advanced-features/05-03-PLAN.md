---
phase: 05-system-refinement-and-advanced-features
plan: 05-03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - backend/src/doctor-toolbox-sync/services/sync-outbound.service.ts
  - backend/src/doctor-toolbox-sync/listeners/sync-outbound.listener.ts
  - backend/src/doctor-toolbox-sync/entities/sync-outbound-log.entity.ts
  - backend/src/doctor-toolbox-sync/doctor-toolbox-sync.module.ts
  - backend/src/patients/services/patient.service.ts
  - backend/src/treatments/services/treatment.service.ts
autonomous: true
requirements: [SYNC-02]
must_haves:
  truths:
    - "Changes to Patients or Treatments in CRM are automatically queued for sync to Toolbox"
    - "Failed sync attempts are automatically retried with exponential backoff"
    - "Audit logs track the status and history of outbound synchronization attempts"
  artifacts:
    - path: "backend/src/doctor-toolbox-sync/services/sync-outbound.service.ts"
      provides: "Sync execution logic"
    - path: "backend/src/doctor-toolbox-sync/listeners/sync-outbound.listener.ts"
      provides: "Event-driven sync triggers"
  key_links:
    - from: "backend/src/patients/services/patient.service.ts"
      to: "EventEmitter2"
      via: "this.eventEmitter.emit('patient.updated', patient)"
    - from: "backend/src/doctor-toolbox-sync/services/sync-outbound.service.ts"
      to: "RetryService"
      via: "exponential backoff integration"
---

<objective>
Implement an event-driven outbound synchronization engine to propagate CRM data changes back to the Doctor Toolbox in real-time.

Purpose:
- Ensure data consistency between the CRM and the external Toolbox system.
- Decouple primary business logic from integration side-effects.

Output:
- Real-time sync triggers for Patient and Treatment changes.
- Robust sync service with retry logic and audit logging.
</objective>

<execution_context>
@/home/hsu/.gemini/get-shit-done/workflows/execute-plan.md
@/home/hsu/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-system-refinement-and-advanced-features/05-RESEARCH.md
@.planning/phases/04-doctor-toolbox-integration/04-02-SUMMARY.md
@backend/src/doctor-toolbox-sync/services/retry.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend - Sync Outbound Infrastructure</name>
  <files>
    - backend/src/doctor-toolbox-sync/entities/sync-outbound-log.entity.ts
    - backend/src/doctor-toolbox-sync/services/sync-outbound.service.ts
    - backend/src/doctor-toolbox-sync/doctor-toolbox-sync.module.ts
  </files>
  <action>
    1. Define `SyncOutboundLog` entity:
       - `id`, `clinicId`, `entityType` (Patient/Treatment), `entityId`, `action` (Create/Update/Delete), `payload` (JSON), `status` (Pending/Success/Failed), `attempts`, `lastError`, `lastAttemptAt`.
    2. Create `SyncOutboundService`:
       - Implement `performSync(logId: string)`:
         - Fetch log record.
         - Call `RetryService.executeWithRetry` to attempt the Toolbox API call.
         - Update log status based on result.
       - Implement Toolbox API client logic (HMAC signature, POST to Toolbox webhook).
  </action>
  <verify>
    <automated>npm run test -- backend/src/doctor-toolbox-sync</automated>
    - Verify `SyncOutboundLog` can be saved and updated.
  </verify>
  <done>
    - Infrastructure for tracking and executing outbound sync is ready.
  </done>
</task>

<task type="auto">
  <name>Task 2: Backend - Event Listeners and Triggers</name>
  <files>
    - backend/src/doctor-toolbox-sync/listeners/sync-outbound.listener.ts
    - backend/src/patients/services/patient.service.ts
    - backend/src/treatments/services/treatment.service.ts
  </files>
  <action>
    1. Update `PatientService` and `TreatmentService` to emit events on Create/Update/Delete (e.g., `this.eventEmitter.emit('patient.updated', patient)`).
    2. Create `SyncOutboundListener`:
       - Use `@OnEvent('patient.*')` and `@OnEvent('treatment.*')`.
       - On event:
         - Create a `SyncOutboundLog` entry with `status: Pending`.
         - Trigger `SyncOutboundService.performSync` asynchronously.
  </action>
  <verify>
    <manual>
      - Create a patient in CRM.
      - Check `sync_outbound_log` table, verify a record exists for this patient.
      - Verify the sync was attempted (status Success or Failed with error).
    </manual>
  </verify>
  <done>
    - CRM changes automatically trigger synchronization workflows.
  </done>
</task>

</tasks>

<verification>
- Backend: Events are emitted correctly and listeners create audit logs.
- Reliability: `RetryService` is used for all outbound network calls.
- Traceability: `SyncOutboundLog` provides clear visibility into sync health.
</verification>

<success_criteria>
- [ ] `SyncOutboundLog` entity tracks all outbound sync attempts.
- [ ] `SyncOutboundListener` responds to patient and treatment lifecycle events.
- [ ] Synchronization uses HMAC-SHA256 signatures for security (matching Toolbox requirements).
- [ ] Failed attempts retry automatically according to `RetryService` policy.
</success_criteria>

<output>
After completion, create `.planning/phases/05-system-refinement-and-advanced-features/05-03-SUMMARY.md`
</output>
