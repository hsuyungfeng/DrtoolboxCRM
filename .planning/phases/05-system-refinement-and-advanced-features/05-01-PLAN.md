---
phase: 05-system-refinement-and-advanced-features
plan: 05-01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/composables/useRevenue.ts
  - frontend/src/views/Revenue/components/RevenueDashboard.vue
  - frontend/src/views/Revenue/components/RevenueRecordsTable.vue
  - frontend/src/views/Revenue/components/RevenueRulesTable.vue
  - frontend/src/views/Revenue/components/RevenueAdjustmentTable.vue
  - frontend/src/views/RevenueView.vue
  - backend/src/common/decorators/clinic-scoped.decorator.ts
  - backend/src/common/guards/clinic.guard.ts
  - backend/src/main.ts
  - backend/src/revenue/controllers/revenue-report.controller.ts
  - backend/src/revenue/controllers/revenue-record.controller.ts
  - backend/src/revenue/controllers/revenue-rule.controller.ts
  - backend/src/revenue/controllers/revenue-adjustment.controller.ts
  - backend/src/patients/controllers/patient.controller.ts
  - backend/src/treatments/controllers/treatment.controller.ts
autonomous: true
requirements: [REF-01, SEC-01]
must_haves:
  truths:
    - "RevenueView.vue is modularized into sub-components"
    - "Shared logic in RevenueView is moved to useRevenue composable"
    - "ClinicGuard enforces clinicId matching between request and authenticated user"
    - "@ClinicScoped() decorator is used to mark controllers for clinic-scoped checks"
  artifacts:
    - path: "frontend/src/composables/useRevenue.ts"
      provides: "Shared revenue logic"
    - path: "frontend/src/views/Revenue/components/RevenueDashboard.vue"
      provides: "Stats and Charts UI"
    - path: "backend/src/common/guards/clinic.guard.ts"
      provides: "Multi-tenant security isolation"
  key_links:
    - from: "frontend/src/views/RevenueView.vue"
      to: "frontend/src/composables/useRevenue.ts"
      via: "composable usage"
    - from: "backend/src/main.ts"
      to: "backend/src/common/guards/clinic.guard.ts"
      via: "global guard registration"
---

<objective>
Refactor the frontend Revenue module for better maintainability and implement a robust backend multi-tenant security mechanism.

Purpose:
- Reduce technical debt in the oversized RevenueView.vue (1300+ lines).
- Ensure strict data isolation between clinics at the framework level.

Output:
- Modularized Revenue UI components and composable.
- Global ClinicGuard and @ClinicScoped decorator.
</objective>

<execution_context>
@/home/hsu/.gemini/get-shit-done/workflows/execute-plan.md
@/home/hsu/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/05-system-refinement-and-advanced-features/05-RESEARCH.md
@frontend/src/views/RevenueView.vue
@backend/src/common/middlewares/clinic-auth.middleware.ts
@backend/src/common/guards/clinic-context.guard.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frontend - Extract Revenue Composable and Modular Components</name>
  <files>
    - frontend/src/composables/useRevenue.ts
    - frontend/src/views/Revenue/components/RevenueDashboard.vue
    - frontend/src/views/Revenue/components/RevenueRecordsTable.vue
    - frontend/src/views/Revenue/components/RevenueRulesTable.vue
    - frontend/src/views/Revenue/components/RevenueAdjustmentTable.vue
  </files>
  <action>
    1. Create `frontend/src/composables/useRevenue.ts`:
       - Move shared reactive state from `RevenueView.vue` (tab state, date ranges, loading flags).
       - Expose methods for data fetching via `revenueStore`.
       - Handle date shortcut logic and tab change events.
    2. Create modular components in `frontend/src/views/Revenue/components/`:
       - `RevenueDashboard.vue`: Extract stats cards (`n-card` with `n-statistic`) and charts (`v-chart`).
       - `RevenueRecordsTable.vue`: Extract records table (`n-data-table`) and column definitions.
       - `RevenueRulesTable.vue`: Extract rules table, column definitions, and "Create Rule" modal logic.
       - `RevenueAdjustmentTable.vue`: Extract adjustments table and audit actions.
    3. Ensure all components use standard project libraries (Naive UI, ECharts).
  </action>
  <verify>
    - `ls frontend/src/views/Revenue/components/*.vue` shows 4 new files.
    - Components compile without errors in a development environment.
  </verify>
  <done>
    - Revenue UI logic and template are cleanly separated into a composable and 4 components.
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend - Refactor RevenueView.vue</name>
  <files>
    - frontend/src/views/RevenueView.vue
  </files>
  <action>
    1. Rewrite `RevenueView.vue` to use the new components and composable.
    2. Use the `useRevenue` composable to manage component state.
    3. Simplify the template to a container with `<n-tabs>` and the modular components.
    4. Remove all extracted logic and local component definitions.
    5. Goal: Reduce file size from 1300+ lines to under 300 lines.
  </action>
  <verify>
    - `grep -c "" frontend/src/views/RevenueView.vue` returns < 300.
    - Manual Check: Access Revenue view in the browser, verify all tabs load data correctly and filters still work.
  </verify>
  <done>
    - `RevenueView.vue` is a lean entry point for the revenue module.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backend - Implement ClinicGuard and @ClinicScoped Decorator</name>
  <files>
    - backend/src/common/decorators/clinic-scoped.decorator.ts
    - backend/src/common/guards/clinic.guard.ts
    - backend/src/main.ts
    - backend/src/revenue/controllers/revenue-report.controller.ts
    - backend/src/revenue/controllers/revenue-record.controller.ts
    - backend/src/revenue/controllers/revenue-rule.controller.ts
    - backend/src/revenue/controllers/revenue-adjustment.controller.ts
    - backend/src/patients/controllers/patient.controller.ts
    - backend/src/treatments/controllers/treatment.controller.ts
  </files>
  <action>
    1. Create `backend/src/common/decorators/clinic-scoped.decorator.ts`:
       - Define `CLINIC_SCOPED` metadata key.
       - Export `@ClinicScoped()` decorator.
    2. Create `backend/src/common/guards/clinic.guard.ts`:
       - Implement `CanActivate`.
       - Check for `CLINIC_SCOPED` metadata on class or handler.
       - If metadata is present:
         - Get `clinicId` from request context (set by `ClinicAuthMiddleware` or from headers).
         - Get `user` from request (set by `JwtAuthGuard`).
         - Verify `request.clinicId === user.clinicId`.
         - Throw `ForbiddenException` if mismatch.
         - Allow `super_admin` role to bypass check.
    3. Update `backend/src/main.ts`:
       - Register `ClinicGuard` as a global guard. Ensure it runs AFTER `JwtAuthGuard` (which is typically registered globally or on controllers). 
       - *Note:* If using `app.useGlobalGuards(new ClinicGuard(reflector))`, ensure `JwtAuthGuard` is also global or that `ClinicGuard` handles cases where `req.user` might be missing gracefully (by skipping or failing).
    4. Apply `@ClinicScoped()` to the specified controllers.
  </action>
  <verify>
    <automated>npm run test:e2e</automated>
    <manual>
      - Log in as User A (Clinic A).
      - Attempt to call an endpoint with `X-Clinic-Id: Clinic_B`.
      - Verify 403 Forbidden is returned.
    </manual>
    <sampling_rate>run after task completes</sampling_rate>
  </verify>
  <done>
    - Security guard implemented and applied to core modules.
  </done>
</task>

</tasks>

<verification>
- Frontend: `RevenueView` functionality remains identical to pre-refactor state.
- Backend: `ClinicGuard` correctly blocks unauthorized cross-clinic data access.
- Code Quality: `RevenueView.vue` is modular and much easier to read.
</verification>

<success_criteria>
- [ ] `RevenueView.vue` < 300 lines.
- [ ] 4 new modular components in `frontend/src/views/Revenue/components/`.
- [ ] `useRevenue` composable handles core logic.
- [ ] `ClinicGuard` successfully intercepts cross-clinic requests in integration tests.
- [ ] `@ClinicScoped()` applied to at least 5 backend controllers.
</success_criteria>

<output>
After completion, create `.planning/phases/05-system-refinement-and-advanced-features/05-01-SUMMARY.md`
</output>
