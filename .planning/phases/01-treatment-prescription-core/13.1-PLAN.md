---
phase: 01-treatment-prescription-core
plan: 13.1
type: execute
wave: 4
depends_on: [13]
autonomous: true
gap_closure: true
files_modified:
  - backend/src/treatments/controllers/treatment-course.controller.ts
  - frontend/src/services/treatments-api.ts
  - backend/src/treatments/controllers/treatment.controller.spec.ts
---

<objective>
修復路由不匹配與測試失敗的問題，達成 Phase 1 完整驗證。

**Problem 1: completeSession 路由不匹配**
- 前端：PATCH /treatments/sessions/:id/complete
- 後端：PATCH /treatment-sessions/:id/complete（路徑前綴不同）
- 影響：完成療程次數按鈕失效（HTTP 404）

**Problem 2: 4 個預先存在的測試失敗**
- 檔案：treatment.controller.spec.ts
- 失敗數：4 個測試因 getCourseWithProgress mock 缺失
- 原因：在計劃開始前就存在的 mock 設定問題

**解決方案：**
1. 在 treatment-course.controller.ts 新增 @Patch('sessions/:id/complete') 路由
2. 在 treatment.controller.spec.ts 的 mock providers 中補充 getCourseWithProgress 方法
</objective>

<execution_context>
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</execution_context>

<context>
前端 TreatmentDetail.vue 含完成療程次數按鈕，呼叫 treatmentsApi.completeSession。
後端 treatment-course.controller.ts 已實作療程管理但缺 completeSession 路由。
</context>

<tasks>

<task type="auto">
  <name>修復 completeSession 路由</name>
  <files>
    - backend/src/treatments/controllers/treatment-course.controller.ts
  </files>

  <read_first>
    - backend/src/treatments/controllers/treatment-course.controller.ts
    - frontend/src/services/treatments-api.ts
    - backend/src/treatments/services/treatment-course.service.ts
  </read_first>

  <action>
在 treatment-course.controller.ts 新增 completeSession 路由：

```typescript
@Patch('sessions/:id/complete')
@UseGuards(JwtAuthGuard)
async completeSession(
  @Param('id') sessionId: string,
  @Request() req: any,
) {
  return await this.treatmentCourseService.completeSession(sessionId);
}
```

確保：
- 路由前綴 @Controller('treatments') → /api/treatments/sessions/:id/complete
- HTTP 方法為 PATCH
- 與前端 treatments-api.ts 完全匹配
  </action>

  <verify>
    - [ ] 檔案存在：test -f backend/src/treatments/controllers/treatment-course.controller.ts
    - [ ] 包含 @Patch('sessions/:id/complete')：grep -q "Patch.*sessions.*complete" backend/src/treatments/controllers/treatment-course.controller.ts
  </verify>

  <done>
- completeSession 路由已新增至 treatment-course.controller.ts
  </done>
</task>

<task type="auto">
  <name>修復 treatment.controller.spec.ts 測試</name>
  <files>
    - backend/src/treatments/controllers/treatment.controller.spec.ts
  </files>

  <read_first>
    - backend/src/treatments/controllers/treatment.controller.spec.ts
  </read_first>

  <action>
補充 getCourseWithProgress 到 TreatmentProgressService mock：

在 mock providers 中找到 TreatmentProgressService 的定義，新增 getCourseWithProgress 方法：

```typescript
{
  provide: TreatmentProgressService,
  useValue: {
    calculateProgressPercent: jest.fn().mockReturnValue(50),
    getProgress: jest.fn().mockReturnValue({
      totalSessions: 10,
      completedSessions: 5,
      progressPercent: 50
    }),
    getCourseWithProgress: jest.fn().mockResolvedValue({
      id: 'mock-id',
      name: 'Mock Course',
      progress: { totalSessions: 10, completedSessions: 5, progressPercent: 50 }
    }),
    isCourseFinallyCompleted: jest.fn().mockReturnValue(false),
  },
},
```

執行測試確保通過：
```bash
npm test -- treatment.controller.spec.ts
```
  </action>

  <verify>
    - [ ] 檔案存在：test -f backend/src/treatments/controllers/treatment.controller.spec.ts
    - [ ] 包含 getCourseWithProgress：grep -q "getCourseWithProgress" backend/src/treatments/controllers/treatment.controller.spec.ts
    - [ ] 測試全部通過：npm test -- treatment.controller.spec.ts 返回成功狀態
  </verify>

  <done>
- treatment.controller.spec.ts mock 已補充
- 所有 4 個測試已通過
  </done>
</task>

</tasks>

<verification>
**路由驗證：**
- backend: PATCH /api/treatments/sessions/:id/complete
- frontend: treatments-api.completeSession 呼叫相同路由 ✓

**測試驗證：**
- treatment.controller.spec.ts 執行結果：全部通過 ✓
- 整體測試覆蓋率：≥ 90% ✓
</verification>

<success_criteria>
- [ ] completeSession 路由與前端匹配
- [ ] treatment.controller.spec.ts 所有測試通過
- [ ] 無新增的路由衝突或 TypeScript 錯誤
- [ ] 所有 13 個真相（Observable Truths）驗證通過
</success_criteria>

<output>
完成後請建立：`.planning/phases/01-treatment-prescription-core/13.1-SUMMARY.md`

所有 gap 修復完成，Phase 1 達成完整驗證。
</output>
