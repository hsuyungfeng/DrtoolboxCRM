---
phase: 01-treatment-prescription-core
plan: 13.1
subsystem: api
tags: [nestjs, jest, treatments, routing, testing]

# Dependency graph
requires:
  - phase: 01-treatment-prescription-core
    provides: TreatmentCourseController, treatment-course.controller.ts, treatment.controller.spec.ts
provides:
  - PATCH /treatments/sessions/:id/complete 路由（與前端完全匹配）
  - 修正 treatment.controller.spec.ts 所有測試通過
affects: [frontend, treatments-api, Phase 2]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - 前端 PATCH /:id/complete 模式對應後端 markSessionComplete handler
    - 測試 mock 使用控制器實際呼叫的 service 方法名稱（getCourseWithProgress 非 getCourseById）

key-files:
  created: []
  modified:
    - backend/src/treatments/controllers/treatment-course.controller.ts
    - backend/src/treatments/controllers/treatment.controller.spec.ts

key-decisions:
  - "markSessionComplete 由 PATCH sessions/:id/complete 呼叫，內部組合 UpdateTreatmentSessionDto 傳遞給 sessionService.completeSession"
  - "測試預期值更新為控制器實際回傳格式：{ statusCode: 200, data: course } 而非裸物件"
  - "TreatmentCourseService mock 補充所有控制器依賴方法（getCourseWithProgress、getPatientCourses 等）"

patterns-established:
  - "Controller 測試 mock 需包含控制器實際呼叫的所有 service 方法"

requirements-completed: []

# Metrics
duration: 5min
completed: 2026-03-27
---

# Phase 01 Plan 13.1: Gap Closure Summary

**PATCH /treatments/sessions/:id/complete 路由新增，修正 4 個因 getCourseWithProgress mock 缺失導致的測試失敗，treatment.controller.spec.ts 全 20 個測試通過**

## Performance

- **Duration:** 5 min
- **Started:** 2026-03-27T07:26:00Z
- **Completed:** 2026-03-27T07:31:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- 新增 `@Patch('sessions/:id/complete')` 路由，與前端 `treatmentsApi.completeSession` 完全匹配（修復 HTTP 404 問題）
- 補充 `getCourseWithProgress` 至 TreatmentCourseService mock，消除所有 `TypeError: getCourseWithProgress is not a function`
- 更新測試預期值符合控制器實際回傳格式（`{ statusCode, data }` 包裝），所有 20 個測試通過

## Task Commits

每個任務皆獨立提交：

1. **Task 1 + 2: 路由修復與測試 mock 修正（合併提交）** - `cf25da65` (fix)

**Plan metadata:** 待建立 docs commit

## Files Created/Modified
- `backend/src/treatments/controllers/treatment-course.controller.ts` - 新增 `markSessionComplete` handler（PATCH sessions/:id/complete）
- `backend/src/treatments/controllers/treatment.controller.spec.ts` - 補充 mock 方法、修正測試預期值

## Decisions Made
- `markSessionComplete` 方法：從 `req.user.clinicId` 取得 clinicId（與其他路由一致），組合 completionStatus + actualEndTime DTO
- 測試 mock provider 補充完整服務方法列表，避免未來新測試因缺少 mock 再次失敗

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 測試預期值不符控制器實際回傳**
- **Found during:** Task 2（測試修復執行時）
- **Issue:** 測試預期 `expect(result).toEqual(mockCourse)` 但控制器回傳 `{ statusCode: 200, data: mockCourse }`
- **Fix:** 更新所有 getCourseById 測試預期值與 integration test 預期值
- **Files modified:** `treatment.controller.spec.ts`
- **Verification:** 20 個測試全數通過
- **Committed in:** cf25da65

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug)
**Impact on plan:** 必要修正，確保測試正確反映控制器行為。無範疇擴增。

## Issues Encountered
- 測試 mock 原始設定呼叫 `getCourseById`（服務舊方法名），但控制器實際呼叫 `getCourseWithProgress`；同時測試預期裸物件但控制器回傳含 statusCode 包裝。兩者皆已修正。

## Next Phase Readiness
- Phase 1 所有缺口已修復，路由與前端完全匹配
- 20 個測試全部通過，無殘留失敗
- Phase 2 可啟動

## Self-Check: PASSED

- treatment-course.controller.ts: FOUND
- treatment.controller.spec.ts: FOUND
- 13.1-SUMMARY.md: FOUND
- Commit cf25da65: FOUND
- Tests: 20/20 passed

---
*Phase: 01-treatment-prescription-core*
*Completed: 2026-03-27*
