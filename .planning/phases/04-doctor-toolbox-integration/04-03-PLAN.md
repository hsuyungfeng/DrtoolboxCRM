---
phase: 04-doctor-toolbox-integration
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - backend/src/doctor-toolbox-sync/services/bulk-export.service.ts
  - backend/src/doctor-toolbox-sync/services/migration-progress.service.ts
  - backend/src/doctor-toolbox-sync/entities/migration-progress.entity.ts
  - backend/src/doctor-toolbox-sync/controllers/migration.controller.ts
  - backend/src/doctor-toolbox-sync/tests/bulk-export.service.spec.ts
  - backend/src/doctor-toolbox-sync/tests/migration.integration.spec.ts
  - backend/src/doctor-toolbox-sync/doctor-toolbox-sync.module.ts
autonomous: true
requirements: [INTEGRATION-01, INTEGRATION-04]
user_setup: []

must_haves:
  truths:
    - "Bulk patient export fetches all patients from Doctor Toolbox for initial clinic setup"
    - "Progress tracking monitors export completion and reports percentage"
    - "Resume capability allows restarting failed exports from last checkpoint"
    - "Error recovery continues after transient failures (network, timeouts)"
    - "Conflicting patients merged automatically (latest data wins)"
  artifacts:
    - path: backend/src/doctor-toolbox-sync/services/bulk-export.service.ts
      provides: "Bulk patient import from Toolbox with conflict resolution"
      exports: ["startMigration", "resumeMigration", "abortMigration"]
    - path: backend/src/doctor-toolbox-sync/entities/migration-progress.entity.ts
      provides: "Progress tracking for long-running exports"
      contains: "clinicId, totalPatients, processedPatients, failedCount, status"
  key_links:
    - from: migration.controller.ts
      to: bulk-export.service.ts
      via: "POST /migrate endpoint triggers startMigration"
      pattern: "bulkExportService\\.start"
    - from: bulk-export.service.ts
      to: migration-progress.entity.ts
      via: "Track progress after each batch"
      pattern: "progressService\\.update"
    - from: bulk-export.service.ts
      to: sync-patient.service.ts
      via: "Reuse conflict detection and merge"
      pattern: "syncPatientService\\.merge"
---

<objective>
Implement initial clinic migration: bulk patient export from Doctor Toolbox on first connection.

Purpose: Enable existing clinics to migrate historical patient data into Doctor CRM with progress tracking and error recovery.

Output:
- BulkExportService (batch import from Toolbox)
- MigrationProgressEntity & MigrationProgressService (track long-running exports)
- MigrationController (POST /migrate endpoint)
- Integration tests (migration with conflicts, timeouts, resume)
</objective>

<execution_context>
@/home/hsu/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/04-doctor-toolbox-integration/04-CONTEXT.md
@backend/src/doctor-toolbox-sync/services/sync-patient.service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: MigrationProgressEntity & MigrationProgressService</name>
  <files>
    backend/src/doctor-toolbox-sync/entities/migration-progress.entity.ts
    backend/src/doctor-toolbox-sync/services/migration-progress.service.ts
  </files>
  <read_first>
    backend/src/doctor-toolbox-sync/entities/sync-patient-index.entity.ts
  </read_first>
  <action>
Create entity (clinicId, totalPatients, processedPatients, failedCount, lastBatchId, status:'pending'|'in-progress'|'completed'|'failed', createdAt, updatedAt).
Create service with methods: startProgress, updateProgress, markComplete, markFailed, getProgress.
All queries include clinicId filter (multi-clinic isolation).
Unit tests verify CRUD and progress calculations (%, eta).
  </action>
  <verify>
    <automated>
      cd backend && npm test -- --testPathPattern=migration-progress.service.spec --coverage
    </automated>
  </verify>
  <acceptance_criteria>
    - MigrationProgressEntity has clinicId, totalPatients, processedPatients, status, timestamps
    - Service methods: startProgress, updateProgress, getProgress
    - All queries include clinicId filter
    - Unit tests pass with ≥ 90% coverage
  </acceptance_criteria>
  <done>
    Progress tracking entity and service fully functional.
  </done>
</task>

<task type="auto">
  <name>Task 2: BulkExportService (batch import with retry & resume)</name>
  <files>
    backend/src/doctor-toolbox-sync/services/bulk-export.service.ts
    backend/src/doctor-toolbox-sync/tests/bulk-export.service.spec.ts
  </files>
  <read_first>
    backend/src/doctor-toolbox-sync/services/sync-patient.service.ts
    backend/src/doctor-toolbox-sync/services/retry.service.ts
    backend/src/doctor-toolbox-sync/services/migration-progress.service.ts
  </read_first>
  <action>
Create service:
  - async startMigration(clinicId: string): Promise<MigrationProgress>
    * Call Doctor Toolbox API GET /patients?clinicId=... with pagination (batch size 50)
    * For each patient: Call syncPatientService.syncFromToolbox (conflict auto-resolved)
    * Update progressService after each batch
    * Wrap API calls in retryService.executeWithRetry (reuse Plan 02 retry logic)
    * On API error (persistent): Mark as failed, return partial progress
  - async resumeMigration(clinicId: string): Promise<MigrationProgress>
    * Find existing migration by clinicId with status != 'completed'
    * Resume from lastBatchId + 1
    * Same batch processing as startMigration
  - async abortMigration(clinicId: string): Promise<void>
    * Mark migration as 'failed', don't delete progress (for audit)
  - Error recovery: Batch timeout 30s per 50 patients
  - Log: Each batch completion, total elapsed time, estimated remaining
Inject: SyncPatientService, RetryService, MigrationProgressService, Logger.
Unit tests: Migration success, conflicts merged, batch timeout with resume, clinicId isolation.
Coverage ≥ 90%.
  </action>
  <verify>
    <automated>
      cd backend && npm test -- --testPathPattern=bulk-export.service.spec --coverage
      grep -n "export class BulkExportService" backend/src/doctor-toolbox-sync/services/bulk-export.service.ts
      grep -n "startMigration\|resumeMigration" backend/src/doctor-toolbox-sync/services/bulk-export.service.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - BulkExportService exported with @Injectable()
    - startMigration fetches all patients from Toolbox API in 50-patient batches
    - resumeMigration continues from lastBatchId
    - Conflict resolution auto-merges duplicates
    - RetryService wraps API calls
    - 30s timeout per batch with recovery
    - Unit tests have ≥ 4 scenarios (success, conflict, timeout, resume)
    - npm test passes with 0 failures
  </acceptance_criteria>
  <done>
    BulkExportService handles long-running migrations with batching and resume.
  </done>
</task>

<task type="auto">
  <name>Task 3: MigrationController & integration tests</name>
  <files>
    backend/src/doctor-toolbox-sync/controllers/migration.controller.ts
    backend/src/doctor-toolbox-sync/tests/migration.integration.spec.ts
  </files>
  <read_first>
    backend/src/doctor-toolbox-sync/services/bulk-export.service.ts
  </read_first>
  <action>
Create controller:
  - POST /migrate/:clinicId → bulkExportService.startMigration(clinicId)
  - POST /migrate/:clinicId/resume → bulkExportService.resumeMigration(clinicId)
  - GET /migrate/:clinicId/progress → migrationProgressService.getProgress(clinicId)
  - DELETE /migrate/:clinicId → bulkExportService.abortMigration(clinicId)
  - Returns MigrationProgress entity with status, percentage, eta
  - Guard: Require auth (use JwtAuthGuard from existing codebase)

Create integration tests:
  - Full migration flow: POST /migrate → 200 {status: 'in-progress'} → poll /progress → {status: 'completed', %: 100}
  - Resume after timeout: Partial import, then POST /resume continues
  - Conflict resolution: Merge 10 existing Toolbox patients into CRM
  - Multi-clinic isolation: Two clinics migrated independently
  Coverage ≥ 90%.
  </action>
  <verify>
    <automated>
      cd backend && npm test -- --testPathPattern=migration.integration.spec --coverage
      grep -n "@Controller('migrate')" backend/src/doctor-toolbox-sync/controllers/migration.controller.ts
      grep -n "POST.*migrate" backend/src/doctor-toolbox-sync/controllers/migration.controller.ts
    </automated>
  </verify>
  <acceptance_criteria>
    - MigrationController has POST /migrate/:clinicId, /migrate/:clinicId/resume, GET /progress, DELETE endpoints
    - JwtAuthGuard applied
    - Returns MigrationProgress DTO with status, percentage, eta
    - Integration tests verify full flow, resume, conflict merge, isolation
    - npm test passes with 0 failures
  </acceptance_criteria>
  <done>
    Migration endpoints and integration tests fully operational.
  </done>
</task>

<task type="auto">
  <name>Task 4: Module registration & E2E test</name>
  <files>
    backend/src/doctor-toolbox-sync/doctor-toolbox-sync.module.ts
    backend/src/doctor-toolbox-sync/tests/migration.e2e.spec.ts
  </files>
  <read_first>
    backend/src/doctor-toolbox-sync/doctor-toolbox-sync.module.ts
  </read_first>
  <action>
Update DoctorToolboxSyncModule:
  - Add MigrationProgressEntity to TypeOrmModule.forFeature
  - Add BulkExportService, MigrationProgressService to providers
  - Add MigrationController to controllers

Create E2E test:
  - Full app bootstrap (NestJS Test module)
  - Real HTTP POST /migrate/:clinicId
  - Verify 200 response with {status: 'in-progress'}
  - Verify MigrationProgress record in database
  - Verify Patient records created from Toolbox data
  Coverage ≥ 90%.
  </action>
  <verify>
    <automated>
      cd backend && npm test -- --testPathPattern=migration.e2e.spec --coverage
      npm run build 2>&1 | grep -i error || echo "Build OK"
    </automated>
  </verify>
  <acceptance_criteria>
    - MigrationProgressEntity in TypeOrmModule.forFeature
    - BulkExportService, MigrationProgressService in module.providers
    - MigrationController in module.controllers
    - E2E test POST /migrate triggers migration
    - E2E test verifies Patient and MigrationProgress records created
    - npm run build succeeds
    - npm test passes with 0 failures
  </acceptance_criteria>
  <done>
    Initial clinic migration system operational with progress tracking and resume capability.
  </done>
</task>

</tasks>

<verification>
## Plan 3 Verification (Wave 2)

1. **Migration initiation:**
   - POST /migrate/clinic-1 → 200 {status: 'in-progress'}

2. **Progress tracking:**
   - GET /migrate/clinic-1/progress → {processed: 50, total: 250, status: 'in-progress', percent: 20}

3. **Resume after timeout:**
   - Partial import (50 of 250), then POST /resume continues from batch 2

4. **Conflict auto-merge:**
   - 10 patients from Toolbox, 5 already in CRM (same idNumber + name)
   - Result: 10 total unique patients (5 new + 5 merged)
</verification>

<success_criteria>
- BulkExportService handles batch imports with 50-patient batches
- MigrationProgressEntity tracks total, processed, failed counts
- Resume capability restarts from last completed batch
- Conflict resolution auto-merges duplicates
- All tests pass with ≥ 90% coverage
- Ready for Plan 4 (audit trail & monitoring)
</success_criteria>

<output>
After execution, create `.planning/phases/04-doctor-toolbox-integration/04-03-SUMMARY.md`
</output>
