# 工作進度暫存（2026-04-19 繼續）

## 🎯 當前任務：P0 生產阻塞項（4 人日規劃）

**總進度**：65% 完成（2.6 / 4 人日）

---

## ✅ 已完成

### Task 1：後端認證安全修復（全部完成，1.7 人日）

**檔案修改**：
- ✅ `backend/src/auth/strategies/jwt.strategy.ts` — 移除硬編碼 secret，強制 `JWT_SECRET` 環境變數
- ✅ `backend/src/auth/auth.module.ts` — 同樣驗證邏輯
- ✅ `backend/src/staff/entities/staff.entity.ts` — 新增 `username` (unique) + `passwordHash` 欄位 + `@Index(['clinicId', 'username'])`
- ✅ `backend/src/auth/auth.service.ts` — 修改 `validatePassword()` 使用 `bcrypt.compare(password, passwordHash)`
- ✅ `backend/src/migrations/1704067200000-AddStaffAuthFields.ts` — TypeORM migration（新建）
- ✅ `backend/scripts/seed-data.ts` — 新增 `import bcrypt` + 建立初始 admin
  - 初始帳號：`username: admin`, `password: InitialPassword123!`, `clinicId: clinic_001`
  - 密碼使用 bcrypt 雜湊存儲

**驗證狀態**：
- JWT secret 強制環境變數 ✅
- 初始 admin 帳號生成 ✅
- bcrypt 驗證邏輯實現 ✅

---

### Task 2：前端登入整合（2/4 子任務完成，0.9 人日）

**子任務 2a：✅ 完成**
- 新建 `frontend/src/types/auth.ts`：LoginDto, LoginResponseDto, UserProfile, RefreshTokenDto
- 新建 `frontend/src/services/auth.service.ts`：authService 物件 (login, logout, profile, refreshToken)

**子任務 2b：✅ 完成**
- 修改 `frontend/src/views/LoginView.vue`
  - 新增 `import authService, useMessage, useI18n`
  - 修改 handleLogin()：呼叫 `authService.login()` 而非 mock
  - 新增錯誤訊息顯示（message.success/error）
  - 新增重導至原始 URL 邏輯 (`router.query.redirect`)
  - clinicId 預設值改為 `clinic_001`

**未完成**：
- ⏳ Task 2c：修改 `frontend/src/stores/user.ts`
  - 需實現 `loginWithApi()` 方法呼叫 `authService.login()`
  - 需實現 `initFromStorage()` 恢復 token + user 狀態
  - 移除 mock `login()` 實作
  
- ⏳ Task 2d：補充 i18n 錯誤訊息
  - 修改 `frontend/src/locales/zh-TW.json`：新增 `auth.loginSuccess`, `auth.loginFailed`, `auth.invalidCredentials`, `auth.sessionExpired`, `auth.networkError`, `auth.serverError`
  - 修改 `frontend/src/locales/en.json`：同英文版本

---

## ⏳ 待完成

### Task 2c：修改 Pinia store（0.5 人日）

**位置**：`frontend/src/stores/user.ts`

**修改內容**：
```typescript
// 新增方法
async loginWithApi(credentials: LoginDto) {
  const response = await authService.login(credentials);
  this.setUser(response.user);
  this.setToken(response.accessToken);
  this.setClinicId(response.user.clinicId);
  return response.user;
}

// 新增應用啟動時恢復狀態
initFromStorage() {
  const token = localStorage.getItem('token');
  const userStr = localStorage.getItem('user');
  if (token && userStr) {
    this.token = token;
    this.user = JSON.parse(userStr);
  }
}

// 移除 mock login() 實作
```

**驗證方式**：
- 頁面重整後 user 狀態應恢復（non-undefined）
- `isAuthenticated` 計算屬性應正確反映登入狀態

---

### Task 2d：補充 i18n 錯誤訊息（0.2 人日）

**修改檔案**：
- `frontend/src/locales/zh-TW.json`
- `frontend/src/locales/en.json`

**新增欄位**（zh-TW）：
```json
"auth": {
  "login": "登入",
  "logout": "登出",
  "username": "使用者名稱",
  "password": "密碼",
  "rememberMe": "記住我",
  "forgotPassword": "忘記密碼？",
  "register": "註冊",
  "loginSuccess": "登入成功",
  "loginFailed": "登入失敗",
  "invalidCredentials": "使用者名稱或密碼錯誤",
  "sessionExpired": "工作階段已過期，請重新登入",
  "networkError": "網路錯誤，請檢查連線",
  "serverError": "伺服器錯誤，請稍後重試"
}
```

---

### Task 3：前端路由認證守衛（0 人日，0/2 子任務）

**子任務 3a**：修改 `frontend/src/router/index.ts`
```typescript
router.beforeEach(async (to, from, next) => {
  // 設置頁面標題
  // 檢查 noAuth 旗標
  // 未登入 → 跳轉 /login，記錄 redirect
  // 檢查角色權限
  // next()
});
```

**子任務 3b**：補充路由 meta
```typescript
{ path: '/login', meta: { title: '登入', noAuth: true } }
{ path: '/patients', meta: { title: '患者管理', requiresAuth: true } }
{ path: '/revenue', meta: { title: '分潤管理', roles: ['admin', 'finance'] } }
```

---

### Task 4：HTTP 安全強化（0 人日，0/2 子任務）

**子任務 4a**：Helmet + CORS
```bash
npm install helmet
```

修改 `backend/src/main.ts`：
```typescript
import helmet from 'helmet';
app.use(helmet({...}));
const corsOrigin = process.env.CORS_ORIGIN?.split(',') || ['http://localhost:5173'];
app.enableCors({ origin: corsOrigin, ... });
```

**子任務 4b**：環境變數統一
修改 `backend/.env.example`：
```dotenv
JWT_SECRET=your-super-secret-key-change-in-production
CORS_ORIGIN=http://localhost:5173,https://example.com
DB_TYPE=sqlite
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=password
DB_DATABASE=doctor_crm
BCRYPT_ROUNDS=10
```

---

## 🔗 相關文檔

- **詳細規劃**：`/home/amd/.claude/plans/deep-research-of-the-nested-eclipse.md`
- **進度追蹤**：Task #2, #3, #4 (in_progress)
- **知識圖譜**：`.planning/graphs/graph.json` (1,061 nodes, 1,112 edges)

---

## 📋 下一步指令（新會話）

```bash
# 繼續 Task 2c + 2d + 3 + 4
"完成 Task 2c（修改 Pinia store）、Task 2d（i18n）、Task 3（路由守衛）、Task 4（Helmet + CORS）"
```

或分步：
```bash
# 只完成 Task 2c-2d
"完成 Task 2c-2d（Pinia store + i18n）"

# 然後單獨執行 Task 3
"完成 Task 3（路由守衛）"

# 最後執行 Task 4
"完成 Task 4（Helmet + CORS）"
```

---

**暫停時間**：2026-04-19 ~11:00（上下文 90% 時）
**預計恢復**：2026-04-19（新會話）
